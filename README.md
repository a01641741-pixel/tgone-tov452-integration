# Integración de telemetría TOV452 — TG One

Documentación técnica de la integración entre **TG One** (plataforma de Total Ground, construida en Base44) y el sistema de telemetría real del medidor **Total View TOV452**, que vive en un servidor MySQL propio del equipo y se expone vía un endpoint PHP.

Este repo existe para que el contrato de la API quede escrito en un solo lugar — antes estaba repartido entre un pizarrón, capturas de Postman y una conversación de chat.

## Estado actual

🟢 **Conectado y en producción.** El 21/jul/2026 Boris confirmó el contrato final (ver abajo) y el equipo ya lo probó en vivo. La función `medicionesReales` está activa en TG One y alimenta la página **"Mediciones Reales"** (`/mediciones-reales`) con datos reales del medidor, sin simulación, refrescando cada 15s.

La ruta abandonada (`tovLive` + token de sesión rotativo, `backend/tovLive.entry.ts` / `frontend/useTovLive.js`) se documenta más abajo solo como historial — Boris optó por un contrato más simple (token fijo, sin login) y esa versión ya no se va a activar.

## Arquitectura (contrato vivo)

```
[Medidor TOV452] → MySQL (tgv_dev, puerto 3306, tabla TOV452_66)
                          ↓
      PHP WS (dbcommapi0099.php, puerto 3030, ruta /tgcommdev/)
                          ↓
     Función medicionesReales en Base44 (proxy server-side, solo lectura)
                          ↓
          Hook useMedicionesReales() en React (TG One → /mediciones-reales)
```

Servidor: `monitor02.redirectme.net:3030`
Endpoint confirmado: `/tgcommdev/dbcommapi0099.php` (⚠️ la ruta anterior `/tgcomm/` sin "dev" dejó de responder — fue la causa de un error 500 real durante las pruebas)

Hay además una app hermana de pruebas ("DB Data Connector", servida en `tgonepostman.site`) con una consola tipo Postman (`src/pages/DbcommQuery.jsx` + función `dbcommQuery`) que documenta el contrato completo (GET/POST/PUT/DELETE) y sirve para probar contra el servidor real sin tocar TG One. Ahí también queda registrado, con candado técnico, que POST/PUT/DELETE siguen bloqueados salvo admin + confirmación explícita.

## Contrato confirmado (21/jul/2026)

Todo viaja por `POST` con un body fijo — ya no hay flujo de login/token rotativo:

```json
{
  "servidor": "localhost",
  "base_de_datos": "tgv_dev",
  "usuario": "root",
  "password": "root",
  "action": "get",
  "token": "Tg#10982278ia123",
  "displayfields": ["Frequency"],
  "tabla": "TOV452_66",
  "condiciones": { "lectura": 8076 }
}
```

Notas del contrato:
- El campo es `action` (minúscula), **no** `Accion` como se pensaba en el diseño V2 original.
- El token es **fijo** (no rotativo, no requiere login previo) y viaja junto con las credenciales en cada llamada.
- `condiciones` acepta la clave en minúscula (`lectura`) aunque la columna real se liste como `Lectura` en las respuestas — MySQL no distingue mayúsculas/minúsculas en nombres de columna, así que ambas formas funcionan.
- Sigue sin exponerse ningún camino de escritura desde TG One: `medicionesReales` solo manda `action: 'get'`, nunca `post`/`put`/`delete`.

## Ejemplo real validado (producción, 22/jul/2026)

Request (replicando exactamente lo que hace `medicionesReales`):

```json
{
  "servidor": "localhost", "base_de_datos": "tgv_dev",
  "usuario": "root", "password": "root",
  "action": "get", "token": "Tg#10982278ia123",
  "tabla": "TOV452_66", "condiciones": { "lectura": 8375 }
}
```

Response real (lectura más reciente al momento de la prueba, `fecha: "2026-07-22 02:14:34"`):

```json
{
  "estado": "éxito",
  "codigo": 200,
  "mensaje": "1 registro(s) encontrado(s).",
  "datos": [{
    "Lectura": 8375, "TOV452_ID": "0004A30B0100D68D", "rssi": -33,
    "fecha": "2026-07-22 02:14:34",
    "Frequency": 5996, "VFase1": 1246, "VFase2": 1208, "VFase3": 1228,
    "IFase1": 0, "IFase2": 0, "IFase3": 0,
    "PF1": 1000, "PF2": 1000, "PF3": 1000,
    "TP1": 400, "TP2": 400, "TC1": 800, "TC2": 5, "kWh": 6,
    "THD_VST1": 2, "THD_VST2": 4, "THD_VST3": 4,
    "THD_IST1": 0, "THD_IST2": 0, "THD_IST3": 0
  }]
}
```

Escalado y sanity-check contra física real: `Frequency` 5996 ÷100 = **59.96 Hz** (red eléctrica real ✓), `VFase1` 1246 ÷10 = **124.6 V** (✓), `PF1` 1000 ÷1000 = **1.000** (✓ a corriente cero). Boris confirmó explícitamente la escala de `Frequency` (÷100, "2 decimales asignados en el módulo de medidores"); el resto de las escalas están verificadas empíricamente contra esta lectura real pero no confirmadas palabra por palabra por Boris — si algún valor se ve raro bajo carga real, hay que volver a validarlas con él.

Nota sobre la lectura `#8076` mencionada por Boris en chat: al consultarla hoy, la API regresa `Frequency: 1000` (no `10000`). Es un registro viejo de prueba — no bloquea nada de lo de arriba, pero si Boris insiste en que debería ser `10000` habría que revisarlo directamente con él del lado de la base de datos (fuera del alcance de este repo).

## Estructura de este repo

**Contrato vivo (usar esto):**
- `backend/medicionesReales.entry.ts` — función server-side (Deno, Base44) activa en TG One. Solo lectura: nunca manda `action` distinto de `'get'`. Soporta `incluirHistorial: true` para sembrar varias lecturas reales recientes en paralelo (ver ítem 22 del checklist).
- `frontend/useMedicionesReales.js` — hook de React que consume `medicionesReales`, hace polling cada 15s, escala los campos crudos a unidades reales (voltaje, corriente, factor de potencia, THD por fase, frecuencia), y siembra `history` con lecturas reales en la primera carga.
- `backend/pruebaCrud.entry.ts` — función server-side que ejercita el contrato de escritura completo (`post`/`put`/`delete`) contra la tabla `prueba` que Boris creó para pruebas. Valida cada campo contra el esquema real de la tabla antes de escribir.
- `frontend/PruebaRegistros.jsx` — pantalla de paginación + CRUD (ruta `/prueba-registros`, solo admin) que consume `pruebaCrud`.
- `backend/guardarLecturaHistorica.entry.ts` — función server-side que persiste snapshots reales de telemetría en la entidad `LecturaHistorica`, para tener tendencias históricas genuinas (no solo la cola en vivo de la tabla externa). Nunca confía en datos del cliente: siempre vuelve a consultar la fuente real antes de guardar, y deduplica por `tabla_bd_externa`+`lectura`.
- `backend/LecturaHistorica.entity.jsonc` — esquema de la entidad donde se guardan esos snapshots (campos crudos, misma convención de escalas que `medicionesReales`).
- `frontend/TendenciasHistoricas.jsx` — componente que lee `LecturaHistorica` y muestra min/prom/máx reales y una curva histórica real, con selector de rango (24h / 7 días / todo). Integrado en el Dashboard Ejecutivo.

**Historial (abandonado, no activar):**
- `backend/tovLive.entry.ts` — intento V2 con token de sesión rotativo y flujo de login. Se dejó `DISABLED = true` a propósito y ya no se va a completar: Boris optó por el contrato más simple de arriba.
- `backend/TovSesion.entity.jsonc` — esquema de caché de sesión para el intento V2. Sigue existiendo como entidad en Base44 pero sin uso activo.
- `frontend/useTovLive.js` — hook que consumía la función anterior; reemplazado por `useMedicionesReales.js`.

### V1 — GET con body (descontinuado, contexto histórico)
El diseño original exigía **método GET con un body JSON crudo**, lo cual viola la especificación de `fetch()` (ningún cliente moderno puede replicarlo) y además el endpoint decidía la operación según el verbo HTTP (`GET`=lectura, `POST`/`PUT`=inserción, `DELETE`=borrado) — un riesgo real, ya que cualquier cliente que adivinara el verbo podía escribir o borrar sin token verificado. Durante esas pruebas se borró por accidente un registro de prueba en `tgv_dev.TOV452_66`. Por eso se descartó, y por lo que ninguna escritura real se expone desde TG One hasta la fecha.

## Campos de la tabla `TOV452_66` (confirmados por inspección de una lectura real en producción)

| Campo | Significado | Escala aplicada |
|---|---|---|
| `Lectura` | Consecutivo de la medición | — |
| `fecha` | Timestamp de la lectura | — |
| `TOV452_ID` | Identificador físico del medidor | — |
| `VFase1/2/3` | Voltaje por fase | ÷10 |
| `IFase1/2/3` | Corriente por fase | ÷1000 (✅ corregido y confirmado por Manuel Vega, Total Ground, 22/jul/2026 — antes se usaba ÷10 por error) |
| `PF1/2/3` | Factor de potencia por fase | ÷1000 |
| `Frequency` | Frecuencia | ÷100 (✅ confirmado por Boris) |
| `THD_VST1/2/3` | THD de voltaje, total por fase | ÷10 |
| `THD_IST1/2/3` | THD de corriente, total por fase | ÷10 |
| `THD_V{h}{fase}` / `THD_I{h}{fase}` | THD por armónico individual (ej. `THD_V13` = fase 1, armónico 3) | sin usar en el dashboard, disponibles si se necesita detalle fino |
| `kWh` | Energía acumulada | sin escala |
| `rssi` | Señal del dispositivo (dBm) | sin escala |
| `TP1/TP2/TC1/TC2` | Relación de transformadores de potencial/corriente (calibración del medidor, no telemetría en vivo) | sin usar en el dashboard |

## Checklist (actualizado)

1. [x] Boris confirma el contrato final (`action` + token fijo + ruta `/tgcommdev/`)
2. [x] Se probó un ciclo completo contra el servidor real y se documentó arriba
3. [x] El código está subido y activo como función `medicionesReales` en Base44
4. [x] La página "Mediciones Reales" muestra el panel completo (frecuencia, voltaje/corriente/PF/THD por fase, kWh, rssi) con datos reales
5. [x] Se creó el registro `Dispositivo` "Total View TOV452" (`tabla_bd_externa = "TOV452_66"`) y se reconectó la página **"Consulta en tiempo real"** (panel "Monitoreo eléctrico") de `useTovLive` (abandonado, siempre deshabilitado) a `useMedicionesReales` (activo) — antes mostraba "datos de ejemplo" aunque la conexión real ya funcionaba en otro lado
6. [x] Se sacó "Consulta en tiempo real" del grupo "Archivo (uso futuro)" del menú lateral (estaba escondida y etiquetada "(legado)" desde que se escribió, cuando todavía no había conexión real) y se movió a la navegación principal, junto a "Mediciones Reales"
7. [x] Se reorganizó el menú lateral para separar sin ambigüedad lo real de lo simulado: grupo **"Telemetría en vivo (real)"** arriba (Mediciones Reales + Consulta en tiempo real), grupo **"Demos de producto (simulado)"** hasta abajo y colapsado — mismas pantallas de antes, ningún dato se perdió, solo se re-etiquetaron y reordenaron para que cualquiera entienda de un vistazo qué es real y qué es mockup
8. [ ] Confirmar con Boris si las escalas de voltaje/corriente/PF/THD (verificadas empíricamente, no palabra por palabra) aplican igual bajo carga real, antes de una demo con inversionistas
9. [x] Rediseño visual del sidebar: el grupo "Telemetría en vivo (real)" ahora tiene una tarjeta con borde/tinte verde y un punto pulsante en el encabezado; el grupo "Demos de producto (simulado)" tiene borde punteado y se ve atenuado; cada item real muestra una píldora "EN VIVO" (en vez de un puntito), cada item demo muestra una etiqueta "SIM"; los íconos ahora viven en chips redondeados para más peso visual. Cero cambios de datos o rutas, solo estilo — verificado con build/lint, no con navegador (no hay acceso a uno en este entorno), así que vale la pena un vistazo rápido en persona antes de la demo.
10. [x] Archivado (no borrado) el video de introducción que se pedía originalmente al inicio de la app: `App.jsx` ahora tiene `const SHOW_INTRO_VIDEO = false;` — `VideoSplash.jsx` sigue intacto, se reactiva cambiando esa constante a `true`. No confundir con la página `/presentacion` (video de marketing, aparte, sigue accesible normalmente).
11. [x] El módulo "Infraestructura" (Locaciones/Áreas/Activos/Dispositivos) solo tenía data de ejemplo genérica (Monterrey, compresores, bombas — nada real). Se creó la cadena real: Locación "Total Ground — Zapopan" → Área "Monitoreo Eléctrico" → Activo "Tablero Eléctrico Principal (TOV452)" → vinculado al `Dispositivo` "Total View TOV452". Los 5 activos genéricos se marcaron `estado: "Dado de baja"` (badge gris neutro, ya soportado por `StatusBadge.jsx` — no dispara notificaciones ni alertas) para que se lean como archivados sin borrar nada. Se probó primero marcar los dispositivos/locaciones de ejemplo como "Inactivo"/"Inactiva", pero esos estados SÍ disparan alertas críticas falsas (`notificationUtils.js`, badge rojo en el sidebar) — se revirtió esa parte para no crear ruido nuevo. También se corrigió el cálculo de "Activos sin Dispositivo" (`Home.jsx`, `ActivosIndustriales.jsx`) para que un activo dado de baja no aparezca como advertencia de "sin monitoreo".
12. [x] La ficha de detalle del dispositivo (`DispositivoDetalle.jsx`) seguía mostrando "Datos de ejemplo — simulados" y "Sin conexión" para el Total View TOV452 real, porque nunca se conectó a `medicionesReales` (todo dispositivo usaba un generador sintético de lecturas/eventos, sin importar si tenía `tabla_bd_externa`). Se corrigió: cuando el dispositivo tiene `tabla_bd_externa`, la ficha ahora jala historial y estado real en vivo. De paso se arreglaron dos bugs preexistentes que afectaban a TODOS los dispositivos: (a) la ficha leía `dispositivo.codigo_interno` pero el campo real se llama `codigo` (por eso siempre decía "Sin código"), y (b) la Locación/Dirección intentaba leer `dispositivo.locacion_id` (campo que no existe en el esquema) en vez de resolverla vía el activo vinculado → área → locación.
13. [x] Se agregó la posibilidad de que un cliente (modo invitado, sin cuenta) solicite atención técnica y agende una cita con un ingeniero — antes el botón "Nuevo Ticket" en `/soporte` estaba oculto para invitados porque no tienen sesión real de Base44. Se creó la función `guestSupportRequest` (usa `asServiceRole`, mismo patrón que `guestRead`) que crea el `Ticket` y, si se da fecha/hora, también la `ReunionSoporte` con enlace de videollamada — superficie mínima a propósito: solo puede crear, no leer/editar/borrar nada más. El formulario de invitado pide nombre/correo/teléfono de contacto y muestra una confirmación con el enlace y clave de acceso si se agendó cita.
14. [x] `AiCopilot.jsx` (TG AI) nunca recibía la telemetría real — su contexto solo tenía activos/dispositivos/tickets/usuarios genéricos. Ahora, si hay un dispositivo con `tabla_bd_externa`, se le pasa `telemetria_electrica_real` (voltaje/corriente/PF/THD/frecuencia por fase, kWh, señal) con instrucción explícita de aclarar que es dato real y nunca inventar valores si no hay lectura. Además, el modelo puede poner `sugerir_soporte: true` cuando el usuario describe una falla real, y el chat muestra un botón "Sí, agendar cita" que abre el mismo flujo de `guestSupportRequest` (invitado) o `Ticket`+`ReunionSoporte` (staff autenticado).

15. [x] En "Consulta en tiempo real" se quitaron los 5 módulos simulados (Sistema de puesta a tierra, Sistema de pararrayos, Telemetría TG One, Supresores de picos, Bancos de capacitores) a petición explícita — la página ahora solo muestra "Monitoreo eléctrico" (el panel real del TOV452). El banner ya no menciona "el resto de los módulos sigue con datos de ejemplo" porque ya no hay resto.
16. Se comparó una foto real del medidor Total View físico (voltajes 121.6/125.8/127.4 V, 6.1 kWh en pantalla) contra la lectura del sistema en el mismo momento (124.4/123.0/125.3 V, 6 kWh) — el rango cuadra (confirma que es dato real, no inventado) pero los valores no coinciden exacto fase por fase. Puede ser fluctuación normal de red entre el instante de la foto y la lectura, o que el orden `VFase1/2/3` en la base de datos no corresponda 1-a-1 con las etiquetas físicas U1/UB/UC del medidor — **pendiente de confirmar con Boris**, no es algo verificable desde el código.
17. [x] "Mediciones Reales" — se verificó que la limpieza pedida (quitar la franja Servidor/Base de datos/Tabla/Refresco y las 3 tarjetas "Solo lectura"/"Fuente real conectada"/"Lectura activa") ya estaba aplicada en el código (commit propio del usuario vía Base44, minutos antes). El RSSI como barras de wifi y los medidores circulares también ya estaban implementados. Sobre eso se agregó:
    - **Bug real de "Curvas en tiempo real" investigado y resuelto**: no era un bug de código — se confirmó empíricamente (llamando al servidor real dos veces con ~80s de diferencia) que el medidor físico a veces deja de mandar lecturas nuevas por más de un minuto. El mensaje "acumulando lecturas…" técnicamente era correcto pero confuso (sonaba a que nunca iba a llegar). Se cambió por un mensaje honesto que muestra el número de la última lectura real y hace cuánto llegó.
    - **Todo lo importante ahora es clickeable**: Frecuencia, Fase 1/2/3 (voltaje) en la lectura principal, los medidores circulares, y las tarjetas de fase (corriente) — cada uno abre un panel de detalle con tendencia real, mínimo/promedio/máximo calculados sobre las lecturas reales acumuladas (nunca inventados).
18. [x] Se confirmó el contrato completo de escritura (INSERT/UPDATE/DELETE) contra el servidor de Boris probando en vivo sobre la tabla `prueba` (creada por él para este propósito, sin tocar `TOV452_66`): `action:"post"` + `campos` inserta, `action:"put"` + `condiciones`+`campos` actualiza, `action:"delete"` + `condiciones` borra — los tres requieren `token`. Se insertó, actualizó y borró un registro de prueba para confirmar el ciclo completo; la tabla quedó exactamente como estaba (21 registros).
19. [x] Se construyó la pantalla que pidió Boris: paginación real (`backend/pruebaCrud.entry.ts`, acción `list`, pagina en memoria sobre la tabla completa porque el servidor no soporta `orden`/`limite`) + CRUD completo con validación por tipo de columna (`frontend/PruebaRegistros.jsx`, ruta `/prueba-registros`, solo visible para admin en el menú "Sistema"). Valida en cliente y servidor contra las reglas reales de la tabla: `Nombre` obligatorio ≤30 car., `Descripcion`/`Status`/`nivel` opcionales (≤30/10/10 car.), `boleano` entero 0-1, `numerico` entero rango `int`, `fecha` formato `YYYY-MM-DD`, y **`doble` entero de -128 a 127 porque la columna real es `tinyint`, no decimal, a pesar del nombre** (esto se marca explícitamente en la etiqueta del campo en el formulario para que nadie se confunda). Solo usuarios con sesión real (no invitados) pueden usar la función — igual que con `medicionesReales`, nunca se expone escritura a un visitante anónimo.
20. [x] **Corregido bug de modo claro** en "Mediciones Reales" → "Lectura principal": los tiles digitales (`DigitalTile`) usaban `bg-zinc-950` fijo (fondo casi negro sin importar el tema), con `dark:bg-black/60` como único ajuste — en modo claro se veían "fondos negros horribles". Se cambió a `bg-muted/50 dark:bg-black/60` (con bordes/textos igualmente adaptados a ambos temas).
21. [x] Simplificado el renglón "Última actualización": ya no muestra la hora exacta (`11:56:30 a.m. · justo ahora`), solo el tiempo relativo (`justo ahora`), a petición explícita. Se eliminó la función `fmtTime`, que quedó sin uso.
22. [x] **Bug real de "Curvas en tiempo real" resuelto de fondo** (reporte de un cliente vía el usuario: "Tenemos 1 lectura real (#8465, justo ahora). El medidor todavía no manda una segunda…"). Diagnóstico previo (ítem 17) ya había confirmado que no era un bug de cómputo sino de UX: cada carga de página arrancaba `history` con un solo punto y dependía de esperar, en vivo, a que el medidor mandara una lectura nueva — y el medidor físico manda lecturas de forma irregular (a veces cada ~15-20s, a veces con huecos de más de un minuto), así que la gráfica se veía "atorada" mucho rato aunque todo funcionara bien. Se resolvió sembrando el historial con datos reales ya existentes en el servidor:
    - `medicionesReales/entry.ts` acepta ahora `incluirHistorial: true` (solo en la carga inicial de la página, nunca en el polling normal) y, en ese caso, trae en **paralelo** las últimas ~8 lecturas reales (`Promise.all` de fetches individuales por `condiciones: { lectura: N }`) en vez de solo la más reciente.
    - Medido en vivo contra el servidor real: 8 fetches en paralelo tardan **~0.3s en total** (vs. ~28s si se pidieran todos los campos de las ~1560+ filas de la tabla de una sola vez) — no añade demora perceptible a la carga de la página.
    - `useMedicionesReales.js` manda `incluirHistorial: true` únicamente en su primera llamada (`primeraCargaRef`) y, si el servidor regresa `historial`, siembra el arreglo `history` completo con esos registros reales (nunca inventados/interpolados) en vez de esperar a que lleguen uno por uno.
    - El mensaje de respaldo ("Tenemos 1 lectura real…") se deja intacto como lo que ahora es: un caso de borde genuino (tabla con muy pocas filas reales), no el camino común.

23. [x] **Corregida la escala de corriente (IFase1/2/3)**: Manuel Vega (Total Ground) confirmó por chat el 22/jul/2026 que el valor de corriente estaba mal — se le aplicaba ÷10 y debía ser ÷1000. Se corrigió en `escalarRegistro()` (`useMedicionesReales.js`) y Manuel confirmó que con ÷1000 el valor ya está bien. El resto de las escalas (voltaje ÷10, PF ÷1000, frecuencia ÷100, THD ÷10) no cambian.
24. [x] **Nuevo "Dashboard Ejecutivo"** (`src/pages/DashboardEjecutivo.jsx`, ruta `/dashboard-ejecutivo`, primer item del grupo "Telemetría en vivo (real)" en el menú) a petición de Sergio (dueño del proyecto): pidió un dashboard con "mucha información y muchas gráficas", con estética cuidada y líneas que se vean en constante movimiento para dar efecto de tiempo real. Se construyó combinando telemetría real del TOV452 con datos operativos reales (Dispositivo/Ticket/ActivoIndustrial/VinculacionDispositivo), todo vía `useMedicionesReales` y `fetchMany` — **nada inventado**:
    - `useMedicionesReales({ tabla: 'TOV452_66', intervalMs: 5000 })` — refresco cada 5s (más rápido que los 15s de "Mediciones Reales") para que las curvas se vean vivas; sigue siendo polling real, nunca datos sintéticos.
    - 6 tarjetas de KPI en vivo (frecuencia, voltaje promedio, corriente total, factor de potencia promedio, señal RSSI, energía) con minigráfica real de fondo.
    - Curvas de área (voltaje y corriente por fase), línea de doble eje (frecuencia + PF), barras agrupadas (THD-V vs THD-I por fase) y un radar de "huella armónica" (las 6 series THD reales en un solo radar, mismo % en las 6, sin normalizar ni mezclar unidades distintas — se descartó a propósito un radar que mezclara voltaje/corriente/PF porque hubiera requerido normalizar contra rangos supuestos, no medidos).
    - Dona de dispositivos por estado y dona de tickets por prioridad (conteos reales de las entidades Base44), más un indicador de cobertura de monitoreo (% de activos industriales con dispositivo vinculado).
    - Feed de las últimas 10 lecturas reales acumuladas (tabla compacta), distinto del "Registro completo" de Mediciones Reales (que muestra 1 registro con todos los campos crudos) — aquí se muestran varias lecturas con columnas clave.
    - El "efecto de tiempo real" (marquesina de valores con scroll continuo, borde con pulso suave en el ícono del encabezado) es decoración CSS pura sobre datos reales — no se fabrica ningún punto ni se acelera el reloj de las lecturas para simular movimiento.

25. [x] **Corregido un bug real de animación que podía dar la impresión de datos falsos "subiendo y bajando"**: el usuario pidió explícitamente que todo movimiento visual sea real, basado en las lecturas reales. Al revisar el código se encontró que varias gráficas de Recharts (áreas/líneas/barras/radar/dona en `DashboardEjecutivo.jsx`, el medidor circular `RadialBar` en `PhaseGauge` de `MedicionesReales.jsx`, y la barra `Bar` del historial en `DispositivoDetalle.jsx`) no tenían `isAnimationActive={false}`. Como el hook `useMedicionesReales` genera un objeto/array nuevo en cada sondeo (cada 5s o 15s según la página) **aunque el valor real no haya cambiado**, Recharts detectaba una "nueva data" y repetía su animación de entrada/barrido — dando la impresión visual de movimiento aunque el medidor físico no hubiera mandado nada nuevo. Se corrigió agregando `isAnimationActive={false}` a todas esas series (ya era el patrón usado en los `Line` de `MedicionesReales.jsx` y `DispositivoDetalle.jsx`, que sí estaban bien desde antes). Ahora cualquier movimiento visual en toda la app corresponde exclusivamente a un cambio real y verificable en los datos.
26. [x] Con las animaciones de redibujado apagadas (ítem 25), las curvas se quedaban visualmente "congeladas" entre lecturas reales (el medidor a veces tarda más de un minuto en mandar la siguiente) — el usuario pidió que se sintiera movimiento constante de todos modos, sin volver a inventar datos. Se agregó un **marcador pulsante** (`makeLiveDot`, animación SVG nativa `<animate>` sobre el último punto real ya graficado) en las curvas de Voltaje/Corriente/Frecuencia+PF de `DashboardEjecutivo.jsx` y en las de "Curvas en tiempo real" de `MedicionesReales.jsx` — es un efecto puramente decorativo (un anillo que pulsa sobre el punto más reciente) que transmite "esto sigue vivo y conectado" sin mover ni un pixel la posición real del dato. Se ajustaron los márgenes de esas gráficas para que el anillo no se recorte contra el borde. La marquesina de valores reales (ya existente en `DashboardEjecutivo.jsx`) sigue siendo la otra fuente de movimiento constante — texto real en scroll continuo, no gráficas fabricadas.
27. [x] **Bug real corregido: la foto del dispositivo no hacía nada visible al hacer clic** (reportado por el usuario en la ficha de un dispositivo, `/dispositivos/:id`). Causa raíz: el diálogo de zoom (`DispositivoDetalle.jsx` línea ~330) renderiza el componente `Image` (`src/components/ui/image.jsx`) con `fittingType="fit"` y solo la clase `w-full`, sin alto ni `aspect-ratio` — como las dos etiquetas `<img>` internas de `Image` están en `position: absolute`, no aportan altura al contenedor en flujo normal, así que el `<span>` envolvente colapsaba a **0px de alto**. El diálogo sí se abría (el botón de cerrar y el título accesible estaban ahí), pero la foto ampliada era invisible — exactamente la sensación de "no pasa nada al hacer clic". Era el único uso de `fittingType="fit"` en toda la app sin una altura fija en el contenedor (el thumbnail chico sí funciona porque su botón padre tiene `h-20` fijo). Se corrigió agregando `h-[70vh] max-h-[600px]` a la clase del diálogo ampliado.
28. [x] **Historial inicial más rico**: se aumentó `NUM_HISTORIAL` de 8 a 20 en `medicionesReales/entry.ts` — confirmado en vivo que 20 lecturas reales en paralelo (con los 19 campos completos) tardan ~1.2s en total, sin bloquear la carga. Con solo 8 lecturas la variación real casi no se notaba en las curvas; con 20 los picos y valles genuinos del medidor son visibles desde el primer render, sin esperar a que se acumulen con el polling en vivo.
29. [x] **Dominios de eje Y con margen real**: las curvas de Voltaje/Corriente/Frecuencia (`DashboardEjecutivo.jsx` y "Curvas en tiempo real" de `MedicionesReales.jsx`) usaban `domain={['auto','auto']}`, lo que pega el pico o valle real justo contra el borde de la gráfica. Se agregó `paddedDomain()` — calcula `[mín - margen, máx + margen]` sobre los valores reales ya mostrados (margen = 8% del rango real observado, nunca un valor inventado) — para que la variación real se lea con claridad.
30. [x] **Bug sistémico real corregido: campos fantasma `estado_conexion` y `codigo_interno`**. Al revisar a fondo el área de Dispositivos (por indicación de Manuel/Boris de enfocarnos ahí) se encontró que 6 archivos (`Dispositivos.jsx`, `Home.jsx`, `ReportGenerator.jsx`, `KpiDetailDialog.jsx`, `DispositivoDetalle.jsx` en su rama de dispositivos sin telemetría real, y `AiCopilot.jsx`) leían/escribían campos `estado_conexion` y `codigo_interno` que **no existen en el esquema real** de `Dispositivo` (los campos reales son `estado` — enum `Activo`/`Inactivo`/`En configuración`/`Con falla` — y `codigo`). Esto significaba que, silenciosamente:
    - La columna "Estado" y "Código" en la lista de Dispositivos siempre aparecía vacía.
    - El KPI **"Dispositivos Offline" en el Centro de Control (`Home.jsx`) siempre marcaba 0**, sin importar cuántos dispositivos reales tuvieran falla.
    - El formulario de alta/edición de Dispositivo dejaba elegir un estado de conexión ("Online"/"Offline"/etc.) que nunca se reflejaba en el campo real `estado` que sí usan las alertas, badges y `notificationUtils.js` en el resto de la app.
    - TG AI recibía `estado: undefined` para todos los dispositivos en su contexto.
    Se corrigieron los 6 archivos para usar `estado`/`codigo` consistentemente, con el enum real, y el filtro "offline" ahora usa el mismo criterio que `notificationUtils.js` (`estado === 'Con falla' || estado === 'Inactivo'`) para que el KPI, las alertas del sidebar y el feed de eventos concuerden entre sí.

31. [x] **Mapa de locaciones corregido: era falso, ahora es real**. El usuario reportó que "el mapa de locaciones no es real, no está operando y no funciona". Se confirmó revisando `src/components/ops/OpsMap.jsx`: no era un mapa real en absoluto, sino una proyección lineal casera de lat/lng sobre un `<div>` con degradado decorativo y anillos de "contorno" falsos — nunca mostraba geografía real de fondo (ni calles, ni costa, ni nada reconocible). Lo notable: **`react-leaflet` (y su dependencia `leaflet`) ya estaban instalados en el proyecto pero nunca se usaban en ningún archivo** — el mapa real estaba a un `import` de distancia y nadie lo conectó. Se reescribió `OpsMap.jsx` para usar `react-leaflet` con tiles reales de OpenStreetMap (gratis, sin API key): `MapContainer` + `TileLayer` (servidor público `tile.openstreetmap.org`, con la atribución que exige su política de uso) + `CircleMarker` por locación (coloreado por el mismo estado de salud que antes: verde/amarillo/rojo/gris), con `FitBounds` para encuadrar automáticamente todas las locaciones reales. Se agregó `leaflet` como dependencia explícita en `package.json` (antes solo estaba como dependencia transitiva de `react-leaflet`). Verificado en vivo con `curl` que el servidor de tiles responde con imagen real (38.5 KB, con calles y etiquetas) para las coordenadas reales de Total Ground — Zapopan (20.64296, -103.42794) — no un tile en blanco.

32. [x] **Nueva entidad `LecturaHistorica` + tendencias históricas reales persistentes**, a petición explícita del usuario de seguir creciendo la plataforma con algo "absolutamente real". Hasta este punto, "picos y valles" solo cubrían lo acumulado desde que se abría la página (o las ~20 lecturas sembradas al cargar) — sin profundidad histórica real de días/semanas. Se construyó un pipeline completo:
    - **Entidad nueva `LecturaHistorica`** (ver `backend/LecturaHistorica.entity.jsonc`): guarda snapshots reales (campos crudos, misma convención de escalas que `medicionesReales`), deduplicados por `tabla_bd_externa` + `lectura`. RLS: lectura abierta, escritura solo vía `asServiceRole` (la función), nunca expuesta a creación directa desde el cliente.
    - **Backfill real inicial**: se consultó la tabla externa `TOV452_66` y se confirmó que ya existían **16 días reales de historia** (6 al 22 de julio de 2026, ~1622 filas). Se seleccionaron 33 lecturas reales distribuidas en buckets de 6h a lo largo de todo ese rango (algunos buckets sin lectura son huecos reales del medidor, no se rellenaron), se trajeron en paralelo (~1.5s) y se insertaron tal cual en `LecturaHistorica` — 100% lecturas reales ya existentes en el servidor, cero inventadas.
    - **`backend/guardarLecturaHistorica.entry.ts`** (nueva función): guarda snapshots hacia adelante. A propósito **nunca confía en datos que mande el navegador** — recibe solo `{ tabla }` y ella misma vuelve a consultar la fuente real (mismo contrato de solo lectura que `medicionesReales`) antes de guardar, con deduplicación server-side. Esto cierra un posible hueco de integridad: nadie puede forjar "historial falso" llamando a la función con un payload arbitrario, porque el payload no importa.
    - **`useMedicionesReales.js`** ahora llama a `guardarLecturaHistorica` en segundo plano (fire-and-forget, sin bloquear la UI) cada vez que detecta una lectura real genuinamente nueva — así el historial sigue creciendo mientras alguien tenga la app abierta, sin depender de un cron server-side (Base44 no expone uno a través de las herramientas disponibles; depender de la propia infraestructura de este asistente para eso habría sido una dependencia oculta y fragil, así que se descartó a propósito).
    - **`frontend/TendenciasHistoricas.jsx`** (nuevo componente, integrado en el Dashboard Ejecutivo): lee `LecturaHistorica`, calcula mín/prom/máx reales de voltaje/frecuencia/corriente y grafica una curva histórica real, con selector de rango (24h / 7 días / todo). Incluye una nota honesta explícita: esto no es monitoreo continuo 24/7, solo se guarda una lectura cuando alguien tiene la app abierta (más el respaldo inicial), así que puede haber huecos reales — para no dar una falsa impresión de cobertura continua que no existe.
    - Se agregó `LecturaHistorica` al whitelist de `guestRead` para que tanto invitados como personal autenticado puedan ver las tendencias.

33. [x] **Pantalla de Paginación/CRUD de `prueba` llevada al siguiente nivel**, a petición explícita de Boris/el usuario ("que quede pasado de verga"). Sobre la base ya funcional (paginación, alta/edición/borrado con validación por tipo real de columna) se agregó:
    - **Búsqueda real** (`pruebaCrud` acción `list`, parámetro `search`) sobre Nombre/Descripcion/Status/nivel, con debounce de 350ms en el frontend para no golpear la función en cada tecla.
    - **Orden por columna real**: clic en cualquier encabezado ordena asc/desc (parámetros `sortBy`/`sortDir`, resueltos en memoria en el backend igual que la paginación, porque el servidor externo tampoco soporta `orden` — ya confirmado antes con `TOV452_66`).
    - **Selección múltiple + borrado masivo real** (`action: 'bulkDelete'`, nuevo en `pruebaCrud`): borra en paralelo cada id seleccionado contra el servidor real, con confirmación explícita.
    - **Duplicar registro**: precarga el formulario de alta con los datos de una fila existente (sufijo "(copia)" en el Nombre) para crear una copia rápida.
    - **Toggle inline del booleano** directo en la tabla (sin abrir el modal) — manda el registro completo al backend (no solo el campo cambiado) para no pisar el resto de las columnas con `null`, ya que el backend reemplaza `campos` tal cual se le pasan.
    - **Exportar a CSV** real (hasta 1000 filas, respetando la búsqueda/orden activos), generado y descargado 100% en el cliente.
    - **Estadísticas reales** en vivo: total de registros, cuántos tienen el booleano activo, cuántos coinciden con el filtro actual — todas calculadas por el backend sobre los datos reales de la tabla, no inventadas.
    - Contadores de caracteres en tiempo real en los campos de texto del formulario (Nombre/Descripcion/Status/nivel) para que sea obvio cuánto falta antes de topar el límite real de la columna.

34. [x] **"Total View" (`/monitoreo/total-view`) dejó de ser una maqueta de demostración y ahora usa telemetría real**. El usuario reportó: "dice Datos de ejemplo — sitio de demostración. Los quiero reales". Esta página vivía separada del dispositivo real "Total View TOV452" (mismo nombre comercial, pero código distinto: `useSimulated` + `Math.random()` cada 6s, sin relación con `useMedicionesReales`). Como sí existe el equipo real, se reescribió `src/pages/monitor/TotalView.jsx` para consumir `useMedicionesReales({ tabla: 'TOV452_66' })` en vez de datos simulados:
    - Los medidores analógicos (voltaje ×3, frecuencia, factor de potencia) ahora muestran valores reales.
    - Se quitaron "Potencia activa/reactiva" (no existen esos campos en la telemetría real del TOV452 — inventarlos hubiera sido fabricar datos) y se reemplazaron por **Energía acumulada (kWh) y Señal (RSSI)**, que sí son campos reales.
    - La curva de voltaje por fase usa el historial real acumulado (`history`), no una serie sintética.
    - Los "eventos" (antes 3 líneas de texto fijas e inventadas) ahora se calculan sobre las lecturas reales acumuladas: cualquier lectura real donde una fase salga del rango 114-127V aparece como evento real, con su lectura y fecha real — si no hay ninguna, se dice honestamente que no hay eventos, en vez de mostrar algo inventado.
    - El panel de información del dispositivo (`MonitorShell`) ahora recibe datos reales (nombre y código reales del `Dispositivo`, ubicación real resuelta vía Activo→Área→Locación) en vez de un serial/firmware/ubicación ficticios — los campos que no existen de verdad (firmware, conectividad) se dejan honestamente en blanco («—») en vez de inventarse.
    - Se agregó un prop `demo` a `MonitorShell` y a `downloadMonitorReport` (default `true`, sin afectar a los otros 5 módulos que siguen siendo demos simuladas a propósito — Terrómetro, Contador de Descargas, Sensor de Gas, Sensor de Presión, Calidad de Energía, ninguno con un dispositivo real detrás): con `demo={false}` muestran un badge verde "Dato real conectado" y el PDF generado dice explícitamente que son datos reales, no simulados.
    - En el menú lateral, "Total View" se movió del grupo "Demos de producto (simulado)" al grupo "Telemetría en vivo (real)".

35. [x] **Navegación jerárquica real: Empresa → Locación → Área → Activo → Dispositivo**. El usuario compartió una foto de un diagrama de pizarrón (Institución/Empresa → Locación → Activo → Dispositivo) con el comentario "dashboard no lleva a nada, el quiere que siga esta ruta". Se investigó el estado real de cada tramo de esa cadena en el código:
    - `Areas.jsx` y `ActivosIndustriales.jsx` **ya** navegaban al hacer clic en una fila (`onRowClick` a `/areas/:id` y `/activos/:id`), y `AreaDetalle.jsx`/`ActivoDetalle.jsx` ya existían mostrando a los hijos reales de cada uno (Área → sus Activos; Activo → sus Dispositivos vinculados, tickets, eventos).
    - **`Locaciones.jsx` era el único tramo roto**: no tenía `onRowClick`, así que al hacer clic en una locación solo se abría el modal genérico de edición — no había manera de bajar de Locación a sus Áreas. Esto es exactamente lo que el diagrama pedía y lo que el reporte "no lleva a nada" describe.
    - Se agregó `onRowClick={(item) => navigate('/locaciones/${item.id}')}` en `Locaciones.jsx` y una ruta nueva `/locaciones/:id` en `App.jsx`.
    - Se creó `src/pages/LocacionDetalle.jsx` (mismo patrón que `AreaDetalle.jsx`/`ActivoDetalle.jsx`, usando `fetchOne`/`fetchMany` de `src/lib/guest.js`, que ya funcionan en modo invitado y con sesión real): muestra los datos reales de la Locación (nombre, dirección, estado, Institución), la lista real de sus Áreas (filtrando `Area` por `locacion_id`, con conteo real de activos por área) con clic a `/areas/:id`, y los datos de la Institución/Empresa dueña.
    - Se completó el último tramo que faltaba: en `ActivoDetalle.jsx`, los Dispositivos vinculados se mostraban en una lista pero no eran clicables — se agregó navegación real a `/dispositivos/:id` al hacer clic en cada uno (cuidando de no disparar la navegación al usar el botón de eliminar vinculación, con `stopPropagation`).
    - Con esto la cadena completa del diagrama ya es clic-a-clic real de principio a fin: Locación → Área → Activo → Dispositivo. Ningún dato nuevo fue inventado — toda la información mostrada en `LocacionDetalle.jsx` viene de relaciones ya existentes en el esquema real (`locacion_id`, `area_id`, `institucion_id`).
    - Verificado con `npm run lint` (0 errores) y `npm run build` (compila) tras el cambio.

36. [x] **Modo Invitado desactivado por completo (seguridad)**. El usuario reportó: "ya la puse en privada pero..." — pidió que "absolutamente nadie pueda entrar, solo a quienes yo dé de alta puedan verla". Se encontró que, aunque la app se puso en privada desde el panel de Base44, seguía existiendo un acceso paralelo sin autenticación: la ruta pública `/invitado` (`src/pages/Invitado.jsx`) activaba una bandera de `localStorage` que hacía que `src/lib/guest.js` leyera datos reales (Institución, Locación, Área, ActivoIndustrial, Dispositivo, Ticket, Usuario, etc.) a través de la función de backend `guestRead`, la cual usaba `asServiceRole` **sin revisar ninguna sesión** — es decir, cualquiera con el link entraba y veía datos reales sin correo ni contraseña, y el toggle de "privada" de Base44 no cerraba ese camino porque es una función custom, no la puerta de entrada estándar de la plataforma. Se desactivó de raíz:
    - `guestRead` ahora rechaza toda solicitud (403) sin tocar ninguna entidad — no se restauró "leyendo menos", se restauró "leyendo nada".
    - `src/lib/guest.js` se neutralizó: `isGuest()` siempre regresa `false` (ni manipulando `localStorage` se puede reactivar), `enterGuest()` es un no-op, y `fetchList`/`fetchOne`/`fetchMany` siempre usan el SDK normal con sesión real — sin tocar los ~15 archivos que ya importaban estas funciones (el cambio es transparente para ellos).
    - Se eliminó la ruta `/invitado` de `App.jsx` y el archivo `src/pages/Invitado.jsx` (sin uso).
    - Se quitaron los botones que activaban el modo invitado en `Login.jsx` ("Continuar como invitado", "Ver catálogo de productos") y `Presentacion.jsx` ("Ver Catálogo") — ambos llamaban `enterGuest()` antes de navegar.
    - Se dejó intacta `guestSupportRequest` (permite que un visitante sin cuenta cree un ticket de soporte): a diferencia de `guestRead`, es de solo escritura, no expone ningún dato real de vuelta, y funciona como un formulario de contacto público — no es el problema que se reportó.
    - Ya existía, y sigue funcionando exactamente como se pidió, la pantalla real de alta de usuarios: `src/pages/AltaUsuarios.jsx` (`/alta-usuarios`), restringida por backend (`base44/functions/emitirInvitacion/entry.ts`) a un único correo propietario — captura correo + rol y usa la invitación nativa de Base44 (`base44.auth.inviteUser`) para que la persona invitada configure ella misma su primera contraseña por correo; el propietario nunca la ve ni la escribe.

37. [x] **TG AI reparado**: el usuario reportó "le pregunto por novedades y no sabe de qué hablo... tiene que arrojarme reportes enteros, saber de todo, hacer sugerencias". Se investigó `src/components/AiCopilot.jsx` (frontend) y `base44/functions/tgAiQuery/entry.ts` (backend) y se encontraron dos causas reales:
    - **Bug real**: el filtro que arma `dispositivos_offline` comparaba `estado` contra `'Offline'`, `'Alerta'`, `'Sin comunicación'` — valores que **nunca existieron** en el enum real de `Dispositivo.estado` (`Activo`/`Inactivo`/`En configuración`/`Con falla`, ver ítem 30). El conteo de dispositivos con falla siempre daba 0, así que TG AI nunca podía reportar un problema real aunque existiera en los datos. Corregido para comparar contra `'Con falla'`/`'Inactivo'` (mismo criterio que `notificationUtils.js`).
    - **Diseño real**: cada pregunta —incluso "genera un resumen del día"— se enterraba bajo un catálogo completo de 400 productos (`CATALOGO_CONTEXT` + lista de `Producto`) que `tgAiQuery` agregaba siempre al prompt, sin importar si la pregunta era sobre operación o sobre productos, además de pedir contexto de internet (`add_context_from_internet: true`) para datos que ya son privados y reales. Se hizo que el catálogo y el contexto de internet solo se incluyan cuando el frontend detecta (por palabras clave) que la pregunta realmente es sobre productos/catálogo — el resto de las preguntas llegan limpias.
    - Se agregó `actividad_reciente` al contexto que recibe TG AI: los últimos 15 eventos reales (tickets nuevos, dispositivos que entraron en falla, activos/usuarios registrados), con la misma lógica que ya usa el timeline de Inicio — antes TG AI no recibía ningún dato con fecha, así que no tenía forma de responder "¿qué novedades hay?".
    - Se agregaron instrucciones explícitas en el `SYSTEM_PROMPT` para que TG AI use esa actividad reciente al responder sobre "novedades" y para que ofrezca sugerencias accionables basadas únicamente en datos reales cuando la situación lo amerite (dispositivos con falla, activos sin monitoreo, tickets críticos, telemetría fuera de rango) — nunca inventadas.

38. [x] **Interfaz amigable para no ingenieros**: a petición explícita del usuario ("quiero que cualquier persona pueda entender qué es TGOne, cómo se usa, qué se mide, si la medición está en rangos normales, si hay que revisarlos o de plano intervenir"), se le preguntó si prefería ocultar lo técnico detrás de un "modo simple" o mantener todo visible con ayuda en lenguaje simple superpuesta — eligió la segunda opción explícitamente. Se construyó:
    - `src/lib/estadoSalud.js`: única fuente de verdad de un semáforo de 3 estados (normal/revisar/alerta), reutilizando los mismos umbrales reales que ya usaba la app en otros lugares (voltaje 114–127V de `VOLT_ZONES` en `TotalView.jsx`, frecuencia 59.5–60.5Hz, factor de potencia 0.90/0.95, y los mismos "bien"/"mal" de THD-V/THD-I que `riesgoPredictivo.js` cita de IEEE 519/NEMA MG1) — no se inventó ningún rango nuevo.
    - `src/components/EstadoSalud.jsx`: banner grande y versión compacta que traducen ese semáforo a una frase clara ("Todo funciona con normalidad" / "Hay algo que conviene revisar" / "Se requiere intervención") con los motivos reales listados debajo.
    - `src/lib/glosario.js` + `src/components/InfoTecnico.jsx`: un ícono "?" junto a cada término técnico (Voltaje, Frecuencia, Factor de potencia, THD, Corriente, RSSI, kWh) que abre una explicación en español simple más el rango normal real — sin ocultar ni reemplazar el número real.
    - `src/components/AyudaTgOne.jsx`: modal "¿Qué es TG One?" invocable en cualquier momento desde un botón "?" nuevo en el header (`Layout.jsx`) — explica qué es la plataforma, qué mide, y qué significa cada color del semáforo.
    - Todo esto se insertó en `Home.jsx` (banner de estado general de toda la operación, vía `evaluarSistema()`), `MedicionesReales.jsx`, `DashboardEjecutivo.jsx` y `TotalView.jsx` (banner de estado de la lectura actual, vía `evaluarLectura()`, más los íconos de glosario junto a las etiquetas técnicas existentes).
    - Bug propio detectado y corregido en el camino: el ícono de ayuda inicialmente se renderizó como un `<button>` anidado dentro de tarjetas ya clicables (`DigitalTile`/`Fase` en `MedicionesReales.jsx`) — HTML inválido con clics impredecibles. Se corrigió usando un `<span role="button">` con manejo de teclado en vez de un `<button>` real.

39. [x] **Módulos de demostración archivados del menú lateral**: el usuario reportó que mezclar los 5 módulos simulados (Terrómetro, Contador de Descargas, Sensor de Gas, Sensor de Presión, Calidad de Energía) con la telemetría real en el mismo menú "confunde muchísimo", y pidió simplificar. Se quitó el grupo "Demos de producto" completo de `navGroups` en `src/components/Sidebar.jsx` (ya no aparece en el menú ni en el buscador del sidebar) — las rutas y el código de esas 5 páginas (`src/pages/monitor/*.jsx`) se dejaron intactos, sin usar, por si se retoman más adelante con un dispositivo real detrás.

40. [x] **Catálogo interno reemplazado por un enlace al inventario real**. El usuario pidió: "en el apartado de catálogo mejor quita todo lo que llevamos, si alguien quiere conocer todo nuestro catálogo, que al hacer clic lleve al inventario: https://www.totalground.com/catalogo". Se eliminaron `src/pages/Catalogo.jsx`, la ruta `/catalogo` en `App.jsx`, y los 4 componentes que solo existían para esa pantalla (`src/components/catalogo/ProductoCard.jsx`, `ProductoDialog.jsx`, `PortafolioWidget.jsx`, `FamiliaConfig.jsx` — carpeta completa borrada) junto con el widget de portafolio que se mostraba en Inicio. El ítem "Catálogo de Productos" del menú lateral (`src/components/Sidebar.jsx`) ahora es un `<a>` externo (`target="_blank"`, con ícono de salida) directo a `https://www.totalground.com/catalogo`, en vez de una ruta interna — se le agregó un caso especial (`item.external`) en `renderItem` para no forzar un `NavLink` sobre una URL externa. No se tocó la entidad `Producto` ni la función `tgAiQuery`: TG AI sigue pudiendo responder preguntas sobre productos desde el chat (eso es una función de conversación, no "el apartado de catálogo" que se pidió quitar).

41. [x] **TG AI: modo consultor de productos**. El usuario aclaró explícitamente que sí quiere que TG AI "sepa absolutamente todo" y pidió que, cuando alguien le cuente su caso (qué quiere medir), pueda recomendar productos reales. Se amplió `PALABRAS_CATALOGO` en `AiCopilot.jsx` (que decide cuándo pedirle a `tgAiQuery` el catálogo completo) para reconocer frases de necesidad/recomendación además de nombres de producto ("recomiend-", "necesit-", "proteg-", "qué me conviene", "ayúdame a elegir", "quiero medir/monitorear/instalar", etc.) — antes solo reaccionaba a palabras como "catálogo" o "comprar", así que describir un caso de uso nunca activaba el contexto de productos. Se agregó una sección "MODO CONSULTOR DE PRODUCTOS" al `SYSTEM_PROMPT`: cuando el caso es claro, recomienda 1–3 productos REALES tomados únicamente de la lista de productos ya provista (nunca inventados), con una frase de por qué aplican, y los pone también en `resultados` (grupo "Productos recomendados"); si el caso es vago, primero pregunta para precisar en vez de adivinar; si nada en el catálogo real aplica, lo dice con honestidad. Se agregó un ejemplo al mensaje de bienvenida y a las sugerencias de arranque para que la función sea descubrible.

42. [x] **Corregidas falsas alertas de voltaje 0V + banner más compacto**. El usuario reportó, viendo el Dashboard: un banner grande "Se requiere intervención" listando "Voltaje Fase 2 fuera de rango normal: 0.0V" y "Fase 3: 0.0V" — pidió que se viera menos alarmante y más chico, "mejor ponlo como valores a revisar". Diagnóstico real: `evaluarLectura()` (ítem 38) trataba 0V exacto como una lectura real fuera de rango, pero 0.0V en un sistema energizado no es una medición real de bajo voltaje — es el patrón con el que este medidor reporta un canal sin conectar (ej. un tablero monofásico monitoreado con un medidor de 3 fases), igual que otros `null` ya se tratan como "sin dato" en vez de inventar qué significan. Se corrigió `src/lib/estadoSalud.js` para omitir 0V del cálculo (no cuenta como alerta ni como normal), y se agregó una banda intermedia "revisar" (108–133V) alrededor de la banda normal (114–127V) para que una lectura apenas fuera de línea (ej. 111.6V, el caso real reportado) se clasifique como "conviene revisar" en vez de saltar directo a "alerta" — mismo criterio de gradualidad que ya tenían frecuencia/PF/THD. Se aplicó la misma protección a frecuencia (0Hz tampoco es una lectura real posible en un sistema energizado). Además, `EstadoSaludBanner` (`src/components/EstadoSalud.jsx`) se hizo más compacto (menos padding, ícono y texto más chicos) en las 4 pantallas donde se usa.

43. [x] **Corregido: la app no llevaba a la pantalla de login propia, y no recordaba el correo**. El usuario reportó: "no veo la pantalla donde te arroje a iniciar sesión, y la app debe recordar el usuario y contraseña cuando sea el caso". Causa real encontrada en `src/lib/AuthContext.jsx`: `navigateToLogin()` (la función que se dispara cuando la sesión no es válida) llamaba a `base44.auth.redirectToLogin()`, un método del SDK de Base44 que manda a `${appBaseUrl}/login` — la página de login **genérica de la plataforma Base44**, no la pantalla propia de TG One (`src/pages/Login.jsx`, con su marca, su copy "El acceso es solo por invitación", etc.). Por eso el usuario nunca veía "la pantalla" que esperaba: la app lo sacaba a otro lugar. Se corrigió para navegar directo a la ruta propia `/login?returnTo=<página original>`. De paso se conectó `src/lib/authReturnTo.js` (ya existía, con su propia validación de seguridad contra open-redirect, pero nunca se usaba) para que `Login.jsx` regrese al usuario a la página que quería ver, en vez de mandarlo siempre a Inicio. Sobre "recordar usuario y contraseña": se confirmó en el código del SDK (`node_modules/@base44/sdk`) que la sesión ya persiste sola entre visitas vía un token guardado en `localStorage` al iniciar sesión — no hacía falta arreglar eso. Se agregó, además, una casilla real "Recordar mi correo en este dispositivo" en `Login.jsx` que precarga el correo (no la contraseña) la próxima vez — **la contraseña nunca se guarda en el código**, guardar contraseñas en `localStorage` las deja en texto plano legible por cualquier script de la página; para eso ya estaba bien puesto `autoComplete="current-password"`, que es lo que le permite al gestor de contraseñas del propio navegador (Chrome, Safari, etc.) ofrecerse a guardarla de forma segura.

44. [x] **Tendencias históricas reales: corregido "no muestra nada" + efecto de movimiento real**. El usuario reportó que la sección no mostraba ninguna curva (solo las tarjetas de Mín/Prom/Máx) y recordó que las gráficas deben sentirse en tiempo real. Causa real: con exactamente 1 lectura real guardada en el rango seleccionado, `TendenciasHistoricas.jsx` sí dibujaba el `<LineChart>`, pero una curva con un solo punto no se ve como nada (un puntito de 2px) — se agregó un mensaje honesto ("solo hay 1 lectura real... hace falta al menos 2 para dibujar una curva") en vez de un gráfico vacío que parece roto. Para el efecto de movimiento real (sin fabricar datos): el componente antes solo consultaba `LecturaHistorica` una vez al montar y nunca volvía a hacerlo, así que un punto real nuevo guardado en segundo plano (por `guardarLecturaHistorica`, disparado cada vez que el medidor manda una lectura nueva mientras la pantalla está abierta) no aparecía hasta recargar la página a mano. Se agregó un refresco automático cada 60s — la curva ahora se mueve sola cuando hay un dato real nuevo que guardar, nunca antes. Se agregó también el mismo marcador pulsante (`makeLiveDot`) que ya usan `DashboardEjecutivo.jsx`/`MedicionesReales.jsx` sobre el último punto real.

45. [x] **Sidebar rediseñado: menos "desparramado", más organizado**. El usuario reportó que el menú lateral "no se ve bonito ni amigable... todo desparramado". Se identificaron causas concretas de ruido visual en `src/components/Sidebar.jsx` y se corrigieron:
    - Se fusionaron los grupos "Personas" y "Operaciones" (2 secciones con 2-4 ítems cada una) en un solo grupo "Equipo y Soporte" — y el grupo aparte "Administración" (que solo existía para el ítem "Dar de alta usuario", visible nada más para el propietario) se integró como un ítem más dentro de "Sistema" en vez de vivir en su propia sección. De 7 secciones de navegación se pasó a 5.
    - Se quitó el pill animado "EN VIVO"/"SIN SEÑAL" que se repetía en cada uno de los 4 ítems del grupo "Monitoreo en vivo" (además del punto pulsante que ya tenía el encabezado del grupo) — era la misma señal repetida 5 veces en la misma pantalla. Ahora esa señal vive en un solo lugar: el punto pulsante junto al título "Monitoreo en vivo", que además ahora sí refleja el estado real de conexión (`badges.live`, antes siempre se mostraba verde sin importar si había dispositivos activos).
    - Se quitó el recuadro con fondo y borde verde alrededor del grupo "Monitoreo en vivo" (el único grupo con un tratamiento visual distinto a los demás) para que todos los grupos se vean consistentes entre sí.
    - La sección "Favoritos" ya no ocupa espacio permanente con un texto de ayuda cuando está vacía (el caso más común) — solo aparece cuando ya hay al menos un módulo fijado.
    - Verificado con `npm run lint` (0 errores) y `npm run build` (compila) tras el cambio.

46. [x] **🔴 Bug de seguridad real encontrado y corregido: la app renderizaba completa para visitantes sin sesión**. El usuario insistió, después de ya haber publicado los arreglos anteriores de acceso (ítems 36 y 43): "ya lo publiqué y sigue sin haber inicio de sesión, cualquiera puede entrar, no tiene nada de seguridad". Eso obligó a buscar más a fondo, más allá del Modo Invitado (ya cerrado) — y apareció un bug real y anterior a esta sesión, en el mecanismo de autenticación original de la app (`src/lib/AuthContext.jsx`), nada que ver con nada de lo tocado antes:
    - `checkAppState()` primero consulta un endpoint de "public settings" de Base44 que responde 200 **exista o no un token** (existe justamente para poder pintar la pantalla de login antes de iniciar sesión). El código decidía si la sesión era válida solo si había un `token` en absoluto — pero **cuando no había ningún token** (el caso de cualquier visitante nuevo), el código nunca llamaba a verificar identidad y nunca marcaba ningún error: dejaba `authError = null` y `isAuthenticated = false` a la vez.
    - `App.jsx` (`AuthenticatedApp`) solo bloqueaba el render de la app y redirigía a login cuando `authError.type === 'auth_required'` — **nunca revisaba `isAuthenticated` directamente**. Con `authError` en `null`, ese bloqueo nunca se activaba, y el código caía derecho a `return <Routes>...</Routes>`: la interfaz completa de TG One se renderizaba para cualquier visitante sin sesión, sin pedirle nunca correo ni contraseña. (Las llamadas a datos reales sí habrían fallado sin token, pero la persona ya estaba viendo toda la navegación y estructura de la app — exactamente el síntoma reportado: "no hay pantalla de inicio de sesión, cualquiera puede entrar".)
    - Se encontró el mismo patrón exacto por segunda vez en `checkUserAuth()`: si `base44.auth.me()` fallaba con un error que no fuera exactamente `status 401` o `403` (error de red, CORS, timeout), tampoco se marcaba `auth_required` — mismo hueco.
    - Corregido en ambos lugares: ahora cualquier falla al confirmar la sesión (sin token, o `me()` fallando por cualquier motivo) marca `authError = { type: 'auth_required' }` explícitamente.
    - Se agregó además un candado de respaldo en `App.jsx`, independiente de `authError`: justo antes de renderizar las rutas de la app, `if (!isAuthenticated) { navigateToLogin(); return null; }` — para que la seguridad de acceso no dependa de que cada rama de manejo de errores futura esté escrita perfectamente.
    - Verificado con `npm run lint` (0 errores) y `npm run build` (compila).

47. [x] **Investigado: "Corriente total" en 0.00A — confirmado que es un dato real, no un bug**. El usuario reportó, viendo el Dashboard: "Electricidad en uso / Corriente total / 0.00 A / Estable / no está midiendo electricidad". Para no adivinar, se consultó **directamente el servidor real** (`curl` contra `dbcommapi0099.php`, sin pasar por ningún código de TG One) pidiendo el registro más reciente (lectura #12098, `fecha: "2026-09-02 18:23:47"`). El propio medidor confirma, con sus propios campos ya escalados en fábrica (`Fase1_amps`, `Fase2_amps`, `Fase3_amps`, independientes de nuestro `escalarRegistro()`): las 3 fases de corriente están genuinamente en `"0.00"`. No es un bug de escala ni de cómputo — la app está mostrando exactamente lo que el medidor reporta.
    - Hallazgo adicional relevante: en ese mismo registro, `VFase2` y `VFase3` (y sus equivalentes `Fase2_volts`/`Fase3_volts`) también están en `0.00` — solo `VFase1` trae un voltaje real (111.6V). Comparado con el ejemplo real validado de julio (ítem de "Ejemplo real validado" arriba, lectura #8375), que **también** traía `IFase1/2/3: 0`, el patrón es consistente desde julio hasta hoy: parece que esta instalación del TOV452 solo tiene conectado el sensor de voltaje de Fase 1, y el/los clamp(s) de corriente (CT) no están sujetando ningún conductor con carga real — es una pregunta de instalación física del equipo, no algo que se pueda corregir desde el código.
    - Para que "0.00 A" no se lea como una falla del sistema, se agregó una leyenda honesta específica: en vez de decir genéricamente "Estable" (que aplica igual cuando de verdad no cambia un valor distinto de cero), la tarjeta "Electricidad en uso" ahora dice "Sin consumo detectado ahora" cuando el valor es exactamente 0 — mismo dato, mejor explicado. Se agregó un prop `zeroLabel` opcional a `LiveStat` (`DashboardEjecutivo.jsx`) para esto, sin afectar las demás tarjetas.
    - Verificado con `npm run lint` (0 errores) y `npm run build` (compila).

48. [x] **🔴 Bug real corregido: "Dar de alta usuario" fallaba siempre con un 500 mudo**. El usuario reportó "marca error al dar de alta usuario", con la pantalla mostrando únicamente `Request failed with status code 500`. Se rastreó hasta la causa exacta, sin adivinar: la función `emitirInvitacion` llamaba a `base44.auth.inviteUser(...)`, pero en el SDK **ese método y `base44.users.inviteUser(...)` pegan a endpoints distintos** (verificado leyendo el SDK instalado, `dist/modules/auth.js` línea 238 y `dist/modules/users.js` línea 19):
    - `auth.inviteUser` → `POST /apps/{appId}/users/invite-user` — la ruta de la consola de Base44.
    - `users.inviteUser` → `POST /apps/{appId}/runtime/users/invite-user` — la ruta *runtime*, la única que acepta el token que Base44 inyecta en una función hospedada.
    Por eso la API rechazaba cada invitación, el `catch` general la convertía en un 500 genérico y el mensaje real se perdía. Se confirmó además, descargando el paquete `@base44/sdk@0.8.38` (la versión que la función tiene fijada), que esa versión **sí** trae el módulo `users` — no hizo falta subir de versión. También se verificó que `asServiceRole` no expone `users`, así que se usa el cliente de sesión; la autorización real (solo el propietario invita) ya ocurrió antes en la misma función.
    - Se descartó por evidencia la hipótesis de "el correo ya estaba invitado": una consulta a la entidad `User` mostró que la única cuenta existente es la del propietario.
    - **Diagnosticabilidad:** el SDK usa axios, que en cualquier respuesta que no sea 2xx lanza un error cuyo `message` es solo `"Request failed with status code NNN"` — el motivo real viaja en `error.response.data` y se estaba tirando a la basura en los dos extremos. Se agregó `detalleError()` en la función (que sí lee el cuerpo de la respuesta de la API) y se corrigió el `catch` de `AltaUsuarios.jsx` para leer `err.response.data.error`. Ahora un fallo dice qué pasó (correo ya registrado, empresa inexistente, etc.) en vez de un número.
    - ⚠️ **Requiere publicar**: `emitirInvitacion` es una función de backend; el arreglo no surte efecto hasta que se presione *Publicar* en Base44.

49. [x] **Logo de TG One: se quitó el truco de `mix-blend-mode` que dejaba un recuadro negro**. El usuario reportó que en la pantalla de inicio de sesión "el logo se ve mal porque tiene borde negro". Causa real, confirmada inspeccionando el PNG original píxel por píxel: el arte de origen **no tiene transparencia** — es 1024×683 completamente opaco, con **82.5% de sus píxeles en negro puro**. La app fingía la transparencia con `mix-blend-mode` (`screen` en tema oscuro, `multiply` + `invert` en claro). Ese truco solo mezcla contra el grupo de composición del elemento, así que se rompe en cuanto el logo cae dentro de un contenedor que crea su propio contexto de apilamiento — exactamente lo que pasa en el login, donde va dentro de `.animate-blur-in` (una animación con `filter`), y ahí el recuadro negro reaparece.
    - Se atacó el origen en vez de agregar otro parche de CSS: se generaron dos variantes con **transparencia real**, des-premultiplicando el negro (`alpha = max(R,G,B)` y luego dividiendo el color entre ese alfa). Eso reconstruye el borde antialiaseado real del logo en vez de recortarlo duro con un umbral. Se recorta además el relleno negro sobrante (135 px arriba y 181 px abajo).
    - Como tras des-premultiplicar **todo el antialias vive en el canal alfa y no en el color**, el color se aplana a los dos tonos exactos del logo (blanco/rojo para fondo oscuro; `#181A20`/rojo para fondo claro, tomando el `--foreground` real del tema). El PNG comprime mucho mejor sin que cambie nada visible: de 290 KB a ~20 KB por variante.
    - **Segundo intento fallido, y su corrección:** primero se sirvieron como archivos en `public/`. El usuario reportó entonces "el logo de TG One se quitó de la plataforma, se ve como un error" — servidos como assets sueltos dependían de que el pipeline de publicación copiara `public/`, y no llegaron. Se cambió a incrustarlos como *data URI* dentro del bundle (`src/components/logoDatos.js`, generado por `scripts/generar-logo.py`): así no pueden dar 404 y además se ahorran dos peticiones en el primer render, que es justo la pantalla de login. Costo medido: +54 KB en el chunk de entrada (761 KB → 815 KB de JS).
    - El CSS quedó reducido a elegir variante por tema (`.tg-logo-oscuro` / `.tg-logo-claro`), con CSS y no con JS para que nunca se alcance a ver la variante equivocada en el primer render.

50. [x] **Reporte PDF: de un resumen de inventario a un documento de ingeniería**. El usuario reportó que el reporte descargable "da un reporte súper x, no tiene casi información cuando debería ser altamente profesional e ingenieril", pegando la salida completa: 6 KPIs de conteo, "Sin alertas activas" y "Actividad del periodo (0 eventos)". Diagnóstico: el generador solo contaba registros de inventario (locaciones, activos, tickets, equipos) y **no incluía una sola medición eléctrica** — es decir, omitía justamente aquello que hace a TG One. Se reconstruyó:
    - **Nueva sección "Estado eléctrico medido"**, por dispositivo: número de lecturas reales y su rango de fechas, y una tabla de mín/prom/máx por magnitud (tensión, frecuencia, corriente, factor de potencia, THD-V, THD-I, RSSI) con su rango de referencia y su semáforo al lado. Más el delta real de kWh del periodo (es un acumulado tipo odómetro, no un promedio) y el dictamen de `evaluarLectura` sobre la última lectura.
    - **Nueva sección "Hallazgos y recomendaciones de ingeniería"**: cada hallazgo nace de un número realmente medido, cita contra qué criterio se comparó (IEEE 519, banda ±10%, umbral CFE) y propone una acción concreta. Si no hay mediciones, **no se emiten hallazgos** — el reporte dice explícitamente que no infiere conclusiones a partir de datos ausentes.
    - **Inventario detallado** en tablas reales (locaciones con su conteo de áreas; activos con código, número de serie, área y estado; dispositivos con marca, tabla de telemetría, firmware y estado) en vez de solo contadores, más la lista de tickets abiertos.
    - **Sección de metodología**: origen de los datos, los factores de escala del propio equipo, la tabla de rangos con su criterio, y dos advertencias honestas — que el historial **no es un registro continuo 24/7** (se guarda un snapshot cuando la plataforma consulta al medidor, así que hay huecos reales y no se interpolan), y que el documento no sustituye una inspección en sitio.
    - **Honestidad de datos:** un canal que reporta exactamente 0 V en todas las lecturas se marca como *canal sin conectar* y se excluye de los promedios en vez de contaminarlos con ceros falsos (mismo criterio que ya usa `estadoSalud.js`); y "0.00 A con tensión presente" se reporta como un hecho medido a verificar en la instalación física, sin afirmar si es ausencia de carga o una pinza mal puesta.
    - **Deuda técnica pagada de paso:** el PDF se armaba con ~180 líneas de llamadas sueltas a jsPDF y el control de salto de página repetido (con números distintos) en cada bloque — por eso agregar contenido salía caro y el reporte se quedó corto. Ahora hay primitivas reutilizables en `src/lib/reportePdf.js` (portada, sección, tabla que repite encabezado al paginar, párrafo, nota) y toda la paginación vive en un solo lugar. El `catch {}` vacío que ocultaba cualquier fallo del reporte también se corrigió.
    - **Primera prueba automatizada del proyecto** (`scripts/prueba-reporte.mjs`, `npm run test:reporte`): corre el análisis y la composición del PDF sobre **registros reales** de `LecturaHistorica` y verifica los números esperados, los casos vacíos y que el PDF pagine solo. **Atrapó un error real de mi parte antes de entregar**: la primera versión de `hallazgos()` elegía un único "peor valor" de tensión comparando distancias absolutas a cada extremo de la banda, y con V1 entre 111.6 y 123.2 V eso descartaba la excursión baja — que era la única realmente fuera de rango. Ahora los dos extremos se revisan por separado. Verificado además con `npm run lint`, `npm run check:campos` (10 pantallas CRUD, todos los campos existen) y `npm run build`.

51. [x] **🔴 Segundo bug real del alta de usuarios: los permisos no se podían asignar al invitar, y el link no dejaba poner contraseña**. Ya con el envío funcionando (ítem 48), el usuario reportó dos cosas seguidas: primero `Se mandó la invitación, pero no se encontró la cuenta para asignarle permisos` (con el correo sí llegando), y después *"cuando abres el link solo te abre para iniciar sesión pero no te deja configurar tu nueva contraseña ni nada"*. Son dos huecos distintos del mismo flujo:
    - **Causa del primero, verificada contra la base:** Base44 **no crea el registro `User` en el momento de invitar** — lo crea cuando la persona acepta la invitación e inicia sesión por primera vez. Se consultó la entidad `User` justo después de un envío exitoso y seguía existiendo únicamente la cuenta del propietario. Es decir, la línea `User.filter({ email })` inmediatamente después de invitar **nunca** podía encontrar nada: no era un retraso de replicación, reintentar con backoff no habría servido.
    - **Solución:** se creó la entidad `InvitacionPendiente` (correo, permiso, acceso master, empresa, estado, quién invitó y cuándo). `emitirInvitacion` ya no intenta escribir permisos en una cuenta inexistente: deja constancia de lo prometido. La nueva función `aplicarInvitacionPendiente` los aplica en el primer inicio de sesión, enganchada en `checkUserAuth()` de `AuthContext.jsx` y disparada solo cuando la cuenta todavía no tiene `rol_operativo` (para no gastar una llamada en cada arranque).
      - Seguridad: esa función **no recibe qué permisos aplicar**. Deriva el correo de la sesión viva y solo puede cobrar una invitación que el propietario ya emitió a ese correo — nadie puede pedirse permisos a sí mismo ni a nombre de otro. Es idempotente, y marca la invitación como aplicada **después** de escribir los permisos, para que un fallo intermedio deje la invitación pendiente y se reintente al siguiente inicio de sesión, en vez de consumirla sin efecto.
      - Si un reenvío cambia el rol antes de que la persona entre, se actualiza la invitación pendiente en vez de acumular registros contradictorios.
    - **Causa del segundo:** el correo nativo de invitación de Base44 solo lleva a la pantalla de inicio de sesión — en ningún momento ofrece definir una contraseña, así que quien recién llega se queda sin credenciales. Revisando el SDK instalado aparecieron `auth.resetPasswordRequest(email)` y `auth.resetPassword({ resetToken, newPassword })`, y la app **ya tenía** la pantalla `/reset-password?token=...` funcionando. El flujo de "restablecer" sirve igual para establecer la contraseña por primera vez, así que `emitirInvitacion` ahora lo dispara al terminar. Esto es exactamente lo que se pidió desde el principio: *"tú me emites un token de restablecer contraseña (en realidad es para que configure la primera contraseña)"*.
      - Es best-effort a propósito y **se reporta con honestidad**: la invitación ya salió y no se puede deshacer, así que la respuesta trae `correo_contrasena_enviado`, y la pantalla de alta dice o bien "le llegarán dos correos" o bien que ese segundo correo falló y que use "¿Olvidaste tu contraseña?" con el mismo correo. No se finge éxito.
    - **`ResetPassword.jsx`**: estaba íntegramente en inglés ("New password", "Passwords do not match", "Invalid reset link") dentro de una app en español, no validaba longitud mínima, y al terminar redirigía de golpe a `/login` sin confirmar nada (parecía que la pantalla se había recargado sola). Se tradujo, se agregó mínimo de 8 caracteres, una pantalla de confirmación antes de redirigir, y se lee el motivo real del servidor cuando el token expiró. Se consideró distinguir "primera contraseña" de "restablecer" con un parámetro en el enlace, pero **el enlace lo compone Base44, no la app**, así que ese parámetro nunca llegaría: en vez de dejar una rama muerta, el texto se redactó para ser correcto en ambos casos.
    - ⚠️ **Requiere publicar**, y son dos funciones de backend: `emitirInvitacion` (modificada) y `aplicarInvitacionPendiente` (nueva).
    - Verificado con `npm run lint`, `npm run test:reporte` y `npm run build`. El recorrido completo (invitar → correo → crear contraseña → entrar → permisos aplicados) **no se pudo ejercitar de extremo a extremo desde este entorno**, porque requiere recibir dos correos reales y abrir el enlace en un navegador; vale la pena probarlo con una cuenta de prueba antes de dar de alta a alguien de verdad.

52. [x] **Alta de usuarios rehecha con enlace propio, replicando la lógica ya probada de SAI Jalisco**. Tras el ítem 51, el usuario reportó que del correo de invitación *"solo llega este correo y arroja al inicio de sesión"* (el nativo de Base44, en inglés, con un botón "Access app") y dijo: *"antes lo que hacíamos es que al yo seleccionar el correo y el rol, tú me dabas un token (enlace), yo lo compartía y con eso ya podía seleccionar su contraseña"*, señalando que esa lógica **ya existe en la app SAI Jalisco** de la misma cuenta Base44.
    - **Se fue a leer la implementación real en lugar de reinventarla**: `enviarInvitacion`, `validarTokenInvitacion`, `registerUser`, la entidad `RegistroToken` y las pantallas `ResetPassword.jsx` / `Register.jsx` de SAI. El mecanismo probado en producción es: la app emite **su propio** código, guarda solo su SHA-256, y **devuelve el enlace** para que el administrador lo comparta por donde quiera; quien lo abre valida el token, y la cuenta se crea con `auth.register` + verificación por código de 6 dígitos.
    - **Antes de eso se descartó la vía del correo, con evidencia**: se llamó directamente al endpoint real `POST /api/apps/{appId}/auth/reset-password-request` y contesta `200` con `"If an account exists with this email, you will receive a password reset link."` — una respuesta deliberadamente ambigua que **no confirma ningún envío**. Con una cuenta que todavía no existe, no hay razón para creer que manda algo. Depender de eso era construir sobre una suposición.
    - **Lo construido en TG One:**
      - `InvitacionPendiente` extendida con `codigo` (SHA-256 del enlace), `expira` (48 h) y `usado`, más RLS restringida a `admin`. Del código en claro no queda rastro en la base: vive solo en el enlace, y la respuesta de `emitirInvitacion` lo entrega **una sola vez**.
      - `emitirInvitacion` genera el código con `crypto.getRandomValues` (no `Math.random`), arma el enlace con el **origen de la petición** (para no fijar un dominio que quede desfasado), manda un correo **propio, en español y con la marca de TG One** vía `Core.SendEmail`, y devuelve `enlace` + `correo_enviado`. Reemitir una invitación reemplaza la anterior, de modo que el enlace viejo deja de servir.
      - `validarTokenInvitacion` (nueva): compara por hash, rechaza usados/expirados y marca el token como usado **antes** de contestar, para que un reenvío de la misma petición ya no encuentre nada. Es la única función de invitaciones sin sesión — por fuerza, porque quien llega aún no tiene cuenta — y **solo devuelve el correo**: ni el rol ni la empresa viajan al navegador.
      - Pantalla `/activar` (nueva): valida el enlace, muestra el correo **fijo** (no un prellenado editable, para que nadie se dé de alta con una dirección distinta a la invitada), pide contraseña, crea la cuenta y confirma con el código de 6 dígitos.
    - **Se quitó `users.inviteUser`** del flujo. Funcionaba (el correo salía), pero ese correo es justamente el que dejaba a la persona atorada. Queda documentado en el código dónde volvería a ir si Base44 llegara a exigir pre-autorización, y cuál sería el síntoma.
    - **Consecuencia detectada y corregida antes de entregar:** al quitar `inviteUser` ya nadie asignaba el rol de *plataforma* (`admin`/`user`) — `register` crea toda cuenta como `user`. Y sí hay cosas reales que dependen de `role === 'admin'`: el grupo "Sistema" del menú lateral y las reglas RLS de `Auditoria`, `InvitacionPendiente` y `LecturaHistorica`. Se agregó `role` a la escritura de `aplicarInvitacionPendiente`.
    - ⚠️ **Requiere publicar** tres funciones: `emitirInvitacion`, `aplicarInvitacionPendiente` y `validarTokenInvitacion`.
    - Verificado con `npm run lint`, `npm run test:reporte` y `npm run build`. **El recorrido completo no se pudo ejercitar de extremo a extremo desde este entorno** (hace falta abrir el enlace en un navegador y recibir el código por correo). Conviene probarlo con una cuenta de prueba antes de dar de alta a alguien real. El punto de mayor incertidumbre es si `auth.register` está habilitado en una app privada: si no lo estuviera, /activar fallaría al crear la cuenta con el motivo exacto del servidor en pantalla, y la solución sería reponer `inviteUser` como paso previo.

53. [x] **Mapa de operaciones: teselas propias de tema oscuro y marcador tipo radar**. El usuario reportó que *"el mapa que usó base no está funcionando"* y pidió *"algo mejor, que dé una impresión menos obsoleta y se vea futurista"*, aportando la dirección real del equipo: C. Volcán Vesubio 5145, Colli Urbano, 45070 Zapopan, Jal.
    - **Lo que sí se identificó como causa del aspecto viejo:** para el tema oscuro, `OpsMap.jsx` le aplicaba al panel de teselas un filtro CSS `invert(1) hue-rotate(180deg)`. Eso no oscurece un mapa: le invierte los colores, así que los parques verdes salían rosas y todo quedaba lavado. Se eliminó el filtro y ahora se usan las teselas de **CARTO** ("Dark Matter" y "Positron"), que ya vienen diseñadas en oscuro y en claro y no requieren API key. Se elige la variante según el tema real de la app (`use-theme.js`), con una `key` en el `TileLayer` para que Leaflet recargue la capa al cambiar de tema en vez de conservar la anterior. Se comprobó que ambas responden `200 image/png`.
    - **Honestidad sobre el diagnóstico:** se sospechó que el fondo no cargaba porque la URL anterior usaba los subdominios `{s}.tile.openstreetmap.org`, que OSM ha ido retirando. **Esa hipótesis no se pudo confirmar**: consultada desde el servidor, esa misma URL sigue devolviendo `200`. La falla ocurre en el navegador y no se pudo reproducir desde este entorno, así que no se afirma cuál fue. Lo que sí es cierto es que CARTO es un proveedor pensado para uso en aplicaciones y que el filtro de inversión ya no está.
    - **Aspecto:** el marcador dejó de ser un círculo plano y ahora es un marcador tipo radar (punto con halo y dos anillos que se expanden), coloreado por el estado real de la locación. **El pulso es decoración y así está anotado en el código**: no representa ninguna medición. Respeta `prefers-reduced-motion`. Los globos, la atribución y los controles de Leaflet traían colores fijos que en tema oscuro se veían como parches blancos; ahora usan los tokens de la app. El globo muestra la dirección real, el estado, los conteos de activos/dispositivos y un enlace "Cómo llegar". Con una sola locación el mapa abre a zoom 17 (nivel de calle) en lugar de 14, que no dejaba reconocer dónde está instalado el equipo.
    - **Bug real encontrado de paso:** en `Home.jsx`, el estado del mapa se calculaba comparando `a.estado === 'Mantenimiento' || a.estado === 'Fuera de servicio'`, pero el enum real de `ActivoIndustrial` es `"Operativo" | "En mantenimiento" | "Detenido" | "Dado de baja"`. Esos dos valores nunca existieron, así que la condición **jamás se cumplía y el mapa no podía pintar una locación en amarillo**. Corregido contra el enum real, dejando "Dado de baja" fuera a propósito (es un activo archivado, no uno que requiera atención — mismo criterio que el conteo de "activos sin dispositivo"). Es el mismo tipo de error que el ítem de `AiCopilot.jsx` con los estados de dispositivo.
    - **Coordenadas corregidas (556 m de error) y verificadas por dos vías independientes:** la locación tenía guardadas `20.64296, -103.42794`. Al geocodificar la calle contra OpenStreetMap, ese punto no caía dentro del tramo devuelto, así que se le pidió a Alejandro la ubicación exacta y aportó el Plus Code **`JHRG+7P Zapopan, Jalisco`**.
      - Se implementó el decodificador de Open Location Code según la especificación y **se validó primero contra un caso documentado** (`recover_nearest('9G8F+6W', 47.4, 8.6)` → `8FVC9G8F+6W` → 47.3656, 8.5248, que es Zúrich) antes de confiar en su salida. Resolviendo el código corto con la locación de Zapopan como referencia: `75GRJHRG+7P` → **`20.640687, -103.423187`**, con una celda de ~14 × 13 m.
      - **Comprobación independiente:** el *reverse geocoding* de ese punto contra OpenStreetMap devuelve `5145, Calle Volcán Vesubio, Zapopan, Jalisco, 45070` — exactamente la dirección declarada, número incluido. Dos fuentes distintas coinciden, así que el dato se actualizó en la entidad `Locacion`.
      - El punto quedaba fuera del *bounding box* que Nominatim había devuelto antes para "Calle Volcán Vesubio" porque ese resultado era **un solo tramo (`way`) de la calle**, no la calle completa: OSM parte las vialidades en varios segmentos. La sospecha inicial de "85–90 m al norte" se basaba en ese tramo parcial y quedó descartada — el error real era de 556 m, hacia el suroeste.
    - Verificado con `npm run lint` y `npm run build`.

54. [x] **Rediseño visual TGOne+: se rehízo la COMPOSICIÓN de Inicio, no solo su tipografía**. El usuario autorizó implementar el rediseño directamente en la app y, tras una primera tanda, fue explícito: *"La transformación visual que pedí todavía no se percibe. Los ajustes de encabezados, bordes, tipografía y etiquetas no alcanzan el resultado esperado. Necesito que cambies de manera sustancial la COMPOSICIÓN"*. La segunda tanda cambió el reparto del espacio, no el acabado.
    - **Inicio**: de una grilla uniforme de tarjetas a una rejilla `2fr / 1fr` — un bloque protagonista con el equipo y su lectura principal (`PanelEquipo.jsx`, titular en `clamp(2.75rem, 7vw, 4.25rem)` sobre vidrio esmerilado), una columna de copiloto a su derecha (`ColumnaCopiloto.jsx`), una banda de instrumentos y una gráfica ancha abajo. La jerarquía la marca ahora el tamaño del bloque, no el grosor de la fuente.
    - **`src/lib/frescura.js` (nuevo, mirroreado)**: separa tres cosas que la interfaz mostraba igual y que significan cosas distintas — **cuándo se midió** (el `fecha` del medidor), **cuándo lo recibimos** y **si la última consulta fue bien**. Es la corrección directa de dos señalamientos del usuario: *"una consulta reciente no significa una medición reciente"* y *"no presentes la hora de recepción del navegador como hora del servidor"*. `BarraEstado.jsx` ahora dice literalmente "Respondió bien hace N s (desde este navegador)" y, en línea aparte, "Medición: {sello completo}".
    - **`Instrumento.jsx` (nuevo)**: familia de instrumentos que **nunca clasifica sin criterio**. `estadoDe()` distingue `sin_dato`, `sin_umbral` y `no_aplica` en vez de asumir "normal" por omisión, y cada instrumento lleva un `Info` que declara de dónde sale su banda.
    - 🔴 **Etiqueta de procedencia falsa, corregida**: los medidores decían que la banda 114–127 V era "±10% de 127 V". El usuario lo señaló — **no lo es** (±10% de 127 daría 114.3–139.7). Se rastreó el origen hasta `VOLT_ZONES` en `TotalView.jsx`, donde el valor **no tiene derivación documentada**. Se cambió la etiqueta a *"Banda configurada en la plataforma — origen sin documentar, pendiente de confirmar"* y **no se tocó ningún umbral**: cambiar valores de operación durante una intervención visual habría sido exactamente lo que el usuario pidió no hacer.
    - 🔴 **Regresión propia, reportada y revertida**: al aplicar el fondo del nuevo sistema visual le puse `bg-[hsl(var(--nivel-0))]` al `<main>` de `Layout.jsx`, lo que **tapó el mundo con rayos animado** que ya existía (`VideoBackground` en `z-0`). El usuario lo notó: *"antes tenía un mundo con rayos animado, quiero que eso se quede como estaba"*. Se devolvió el `<main>` a fondo transparente, con un comentario en el código explicando por qué no debe volver a pintarse, se restauró `--background` y se borró la clase `.tg-trama`.
    - 🔴 **`useMemo` sin importar en `Home.jsx`** provocó un `ReferenceError` en vivo — el usuario vio *"Algo falló en esta pantalla"*. **El `npm run build` pasó igual**: el bundler no valida identificadores libres. Además de corregir el import, se activó `"no-undef": "error"` en `eslint.config.js` **y se verificó que la regla realmente atrapa el caso** reintroduciendo el error a propósito antes de darla por buena.
    - **Toasts que no se cerraban**: `use-toast.jsx` traía `TOAST_REMOVE_DELAY = 1000000` (16.7 minutos) y `TOAST_LIMIT = 20`, así que la "x" quitaba la tarjeta de la vista pero la dejaba en la cola y se acumulaban. Ahora: límite 3, retiro a 400 ms y auto-cierre a 4 s (8 s los destructivos).
    - Verificado con `npm run lint`, `npm run test:reporte` y `npm run build`. **No hay navegador en este entorno**, así que la composición no se pudo ver renderizada: conviene una captura de Inicio en escritorio y en celular antes de darla por buena.

55. [x] **Modelo 3D real del TOV452, con el LCD mostrando la lectura verdadera y manipulable con el dedo o el ratón**. El usuario aportó un visor propio (`Total_View_TOV452_Viewer_standalone.html`) y pidió: *"este será el modelo 3D que usemos, impleméntalo mucho porque está sumamente novedoso; quita todo lo que diga demostración porque sí es un modelo 3D real"*.
    - **Qué había dentro del archivo, verificado:** se descomprimieron sus cinco bloques JS (zlib) y la geometría resultó **procedural**, no un GLTF/GLB — no existe ningún modelo que importar, hay que construirlo. Se portó a `src/components/dispositivo/modeloTOV452.js`: gabinete, bisel, LCD, botonera UI/M/P/E, cerradura, perilla estriada con su escala, tornillería y placa de marca, como mallas nombradas.
    - 🔴 **Por qué "quitar lo que diga demostración" no era solo borrar un texto:** el LCD del original traía cifras **fijas** ("121.6 V", "125.8 A", "127.4") y encima la leyenda *DEMO — NO CONECTADO*. Borrar la leyenda dejando los números habría convertido una marca honesta en una pantalla que finge medir. Se hizo al revés: **el LCD dibuja ahora la lectura real** que la app está recibiendo, y cuando no hay lectura muestra guiones (`---`), nunca un número de relleno. Recién entonces quitar la marca de demostración dice la verdad. La nota técnica del autor original —que es una representación visual basada en fotografía, fiel en aspecto pero **no un modelo dimensional para tomar cotas**— sí se conservó.
    - **Manipulación libre** (*"me gustó mucho, pero no lo puedo mover a todos lados y esa es justamente la idea"*): se añadió `OrbitControls` con amortiguación, límites de acercamiento acotados a la escala real del gabinete (0.30 × 0.45 m) y traspaso de mando — en cuanto alguien arrastra, el recorrido guiado por scroll se apaga para no pelearse con la mano, y los botones de encuadre (General / Medidor / Perilla) devuelven la cámara a una vista conocida.
    - 🔴 **Trampa táctil detectada y corregida antes de entregar:** `OrbitControls` le fija `touch-action: none` al canvas (confirmado en el fuente instalado, `OrbitControls.js:204`). En un celular, donde el visor ocupa casi toda la pantalla, eso **atrapa el dedo**: el equipo gira pero la página ya no baja. En punteros gruesos (`pointer: coarse`) se restituye `touch-action: pan-y`, así el navegador conserva el desplazamiento vertical de la página y la escena recibe el horizontal, que aquí es el giro que importa. La pista en pantalla se adapta al dispositivo.
    - 🔴 **Segundo hueco corregido:** con `prefers-reduced-motion` no hay bucle de animación (se dibuja un fotograma y ya), así que arrastrar **no habría repintado nada** y el equipo se habría visto congelado justo para quien pidió menos movimiento. Se añadió un oyente `change` que dibuja un fotograma por cada cambio de cámara — movimiento pedido a mano, no animación automática.
    - **Costo de arranque, medido:** `three` + `OrbitControls` viven en un chunk cargado en diferido; se comprobó tras compilar que el bundle de entrada **no contiene `WebGLRenderer`** (0 coincidencias) y que ambos quedan en `Escena3D-*.js`. La escena además detiene su bucle fuera de pantalla y con la pestaña oculta, y si no hay WebGL ofrece la alternativa 2D con los mismos datos.
    - Verificado con `npm run lint` y `npm run build`. **Sin navegador en este entorno no se pudo probar el gesto real**: el reparto de `pan-y` (un dedo horizontal gira, vertical desplaza la página) y el pellizco de dos dedos son lo primero que conviene revisar en un celular.

56. [x] **Verificación en vivo de la conexión, y tres fallas reales encontradas al hacerla — dos de ellas mías**. Alejandro pidió *"revisa la conexión en tiempo real y confírmame que todo está funcionando con normalidad y estamos dando info real"*. La conexión sí funciona y los datos sí son reales, pero **no se podía confirmar que todo estuviera normal**, así que se reportó lo que se encontró y luego se corrigió.
    - **Cómo se verificó, sin arriesgar el servidor.** Se replicó exactamente el cuerpo que manda `medicionesReales` (`action: 'get'` + `displayfields`, **nunca** la clave `campos`, que es la que dispara escrituras en ese endpoint). Respuesta `200` en 477 ms el índice y 120 ms el registro completo; 5,557 lecturas en `TOV452_66`, sin duplicados y con el contador `lectura` casi denso (6 saltos en total). Se respetó la regla vigente desde el incidente de borrado: ninguna operación distinta de lectura.
    - **La cadena completa está viva, y eso se comprobó sin creerle a un solo indicador**: durante la revisión entró una lectura nueva (`02:18:34`) y la plataforma ya la tenía archivada. Eso sólo puede ocurrir si `medicionesReales` → navegador → `guardarLecturaHistorica` corrió completo minutos antes, en producción.
    - **Lectura real del momento** (`lectura` 12464, equipo `0004A30B0100D68D`): 117.5 V en Fase 1, 60.00 Hz, 0.000 A, PF 1.000, THD-V 0.3 %, kWh 10, RSSI −42 dBm. Sobre 60 lecturas consecutivas (6 h 16 min): V1 entre **114.8 y 119.3 V** (promedio 116.8), frecuencia entre 59.95 y 60.04 Hz, señal entre −47 y −31 dBm.
    - **Zona horaria del medidor, deducida y declarada como deducción**: el campo `fecha` viaja sin zona. Comparado contra el `created_date` de Base44 (que sí es UTC), el desfase es consistentemente ~6 h, de modo que **el medidor sella en hora local de Zapopan (UTC−6)**. No es un dato declarado por el equipo — conviene que Boris lo confirme. `frescura.js` interpreta esa cadena en la zona del navegador, lo cual es correcto para un navegador en México y no lo sería fuera; queda anotado en el código.
    - 🔴 **No se está midiendo corriente, y sigue sin resolverse.** `IFase1 = 0.000 A` en **60 de 60** lecturas con tensión presente, y `kWh` congelado en 10 durante las 6 horas. Es el mismo síntoma de julio: falta la revisión física de la pinza amperimétrica (CT). Mientras siga así, corriente, potencia y energía no tienen sustento — y el factor de potencia que la app pinta como 1.000 no describe nada, porque no hay corriente que describir. **Esto no se puede arreglar desde el software.**
    - 🔴 **Umbral de frescura mal calibrado — error mío, medido y corregido.** `frescura.js` dejaba de llamar "reciente" a un dato a los 3 min, pero la cadencia real del medidor es de **~6.4 min** (mediana de 382 s sobre 60 lecturas). Resultado: **58 de 59 intervalos reales superaban el umbral**, así que la app decía "Dato rezagado" prácticamente siempre aunque el equipo estuviera perfecto. Un umbral fijo tampoco podía servir a futuro: cada medidor que se dé de alta puede reportar a su propio ritmo.
      - Se sustituyó por una calibración derivada del historial real de **cada** equipo: `cadenciaDe()` toma la **mediana** de los intervalos (no el promedio) descartando los absurdos, y `umbralesDe()` deriva "al día" y "atrasado" de ahí, con pisos para que un medidor muy rápido no acabe con una ventana de segundos. La interfaz ahora cita el ritmo observado ("Este equipo reporta cada ~6.4 min") y **distingue explícitamente** cuándo lo midió de cuándo lo está asumiendo.
      - El comentario de `medicionesReales` que afirmaba "a veces cada ~15-20 s" tampoco corresponde a lo que el medidor hace hoy.
    - 🔴 **Se estaban guardando lecturas duplicadas — también consecuencia del rediseño.** Al menos **9 fechas repetidas** entre los 379 registros históricos. Verificado un caso concreto: `lectura 12425` guardada **dos veces, con 78 ms de diferencia**. `guardarLecturaHistorica` sí deduplicaba, pero **comprueba y luego escribe**, así que dos llamadas simultáneas pasan ambas la comprobación. La causa: había **seis** instancias de `useMedicionesReales` vivas, y `AiCopilot` vive en el Layout, así que se sumaba a la página que fuera. Todos los duplicados son del 4 y 5 de septiembre — después del rediseño. `TelemetriaContext.jsx` se había creado justamente para esto y **nunca se conectó**.
    - **Un registro del medidor trae la fecha desfasada, y no es único.** `lectura 12459` dice `2026-09-02 18:30:10` mientras sus vecinas inmediatas dicen `2026-09-06 01:33` y `01:46` — 79 h atrás. Se observó el mismo patrón en julio. Viene del medidor, no de la app. Hoy no afecta cuál es la lectura más nueva (`max(lectura)` coincide con la fecha más nueva), pero la app **asume que el contador y el tiempo van juntos** y aquí hay prueba de que no siempre. Por eso `cadenciaDe()` usa la mediana con los saltos filtrados: un solo registro así habría disparado la cadencia a horas y todo habría parecido "al día" para siempre.

57. [x] **Telemetría lista para más de un medidor: sondeo único, tabla ya no escrita a mano y archivado independiente de la pantalla**. Alejandro pidió dejar TG One operativo *"para que los ingenieros puedan seguir añadiendo más dispositivos a la telemetría en tiempo real"*. Lo que impedía eso no era una funcionalidad faltante, sino tres supuestos de "hay un solo equipo" repartidos por el código.
    - **`TOV452_66` estaba escrita a mano en tres pantallas** (`MedicionesReales.jsx`, `monitor/TotalView.jsx`, `DashboardEjecutivo.jsx`). Con un solo medidor funcionaba; en cuanto se diera de alta el segundo, esas pantallas habrían seguido mostrando el primero **sin avisar** — dato real, pero de otro equipo, que es peor que no mostrar nada. Ahora la tabla sale del equipo seleccionado. No queda ninguna tabla fija en el código: las únicas menciones que sobreviven son comentarios y el *placeholder* del formulario de alta.
    - **`TelemetriaProvider` conectado al Layout** (envuelve todo, no sólo el `<main>`, porque el copiloto vive fuera). Es la fuente única: un solo sondeo de vista, y `useMedicionesReales` ganó un parámetro `persistir` que por omisión es `false` — **sólo el proveedor archiva**.
    - **El archivado dejó de depender de qué equipo estés mirando.** Recorre **todas** las tablas dadas de alta, **en serie** (nunca en paralelo: dos llamadas concurrentes sobre la misma tabla son justo la carrera que produjo los duplicados, y en serie tampoco se le echan N peticiones de golpe a un servidor ajeno). Así el histórico de un equipo no deja de crecer porque nadie lo tenga abierto. ⚠️ Limitación anotada en el código: archiva cada 60 s y guarda sólo la lectura más reciente de cada pasada, así que **un medidor que reportara más rápido que 60 s tendría huecos en el histórico** (la vista en vivo sí las mostraría todas).
    - **Dedup a prueba de carreras entre navegadores.** El sondeo único elimina la carrera dentro de una sesión, pero dos navegadores con la app abierta siguen pudiendo chocar, y eso no se arregla desde el cliente. `guardarLecturaHistorica` ahora, después de escribir, revisa y deja una sola fila, conservando **la más antigua** (criterio estable: dos limpiezas simultáneas eligen la misma y no se borran la fila una a la otra). Además acepta `barrerDuplicados`, que el proveedor pide **una sola vez por sesión y por tabla**, para limpiar los duplicados que ya estaban en la base. **El borrado es sobre `LecturaHistorica`, entidad nuestra de Base44 — nunca sobre la base del medidor**, que sigue siendo de sólo lectura desde la app.
    - **`SelectorEquipo.jsx` (nuevo)**: con un equipo muestra su nombre y su tabla; con varios, un desplegable; con ninguno lo dice. Distingue *"no se pudo consultar la lista"* de *"no hay ninguno con telemetría"*, que son dos situaciones distintas. Abrir la ficha de un dispositivo lo vuelve el equipo seleccionado de la sesión, así que el copiloto y las demás pantallas hablan del mismo que se está viendo — antes el copiloto elegía por su cuenta "el primero con tabla".
    - **`scripts/prueba-frescura.mjs` (nuevo, `npm run test:frescura`)**: 21 comprobaciones sobre **marcas de tiempo reales** del TOV452_66, incluida a propósito la lectura 12459 con su fecha desfasada. Fija el comportamiento que motivó el cambio: mide 387 s de cadencia sobre la serie real (coincide con los 382 s medidos aparte sobre 60 lecturas), confirma que el registro desfasado **no** arrastra la medición, que un dato de 5 min ya **no** se marca como rezagado, que uno de 2 h sí sale atrasado, y que sin historial la interfaz **declara** que está asumiendo el ritmo en vez de presentarlo como medición.
    - ⚠️ **Requiere publicar**: `guardarLecturaHistorica` es una función de backend y el barrido de duplicados no corre hasta que se publique.
    - Verificado con `npm run lint`, `npm run test:frescura`, `npm run test:reporte`, `npm run check:campos` (10 pantallas CRUD) y `npm run build`. **Sin navegador en este entorno**, así que no se pudo ver el selector renderizado ni comprobar el recorrido con dos equipos dados de alta — eso conviene probarlo al dar de alta el segundo medidor de verdad.

58. [x] **Archivado en el servidor: el histórico deja de depender de que alguien tenga TG One abierta**. Alejandro pidió resolver esto primero. El archivado corría desde el navegador, así que cerrar la pestaña dejaba huecos permanentes en el histórico.
    - **Qué admite realmente la plataforma, verificado antes de construir**: Base44 tiene **automatizaciones nativas** declaradas en `base44/functions/<nombre>/function.jsonc`, con `type: "scheduled"` y `schedule_type: "cron"` o `"simple"`. **El intervalo mínimo es de 5 minutos.** Se descartó la vía de los *Actors* (`schedule`/`handleWake` del SDK) al leer su contrato: el cliente de un Actor es **anónimo y sujeto a RLS**, y `LecturaHistorica` sólo permite escritura a `admin` — habría hecho falta abrir la entidad o incrustar un secreto, las dos cosas peores. La documentación oficial está bloqueada desde este entorno; el contrato se obtuvo de una copia pública de la referencia `base44-cli`.
    - 🔑 **Identidad de servidor sin secreto compartido**: una automatización programada se ejecuta **autenticada como el usuario que la creó**. Eso permitió exigir sesión con rol `admin` en la función y que la corrida programada la cumpla sola — sin llave compartida, sin sesión humana y sin entrada anónima privilegiada, que era justo lo que había que evitar.
    - **Capacidades reales del origen, medidas (sólo lectura, nunca la clave `campos`)**: `condiciones` es de **igualdad exacta** — una lista de valores o un operador (`>`) **no fallan: devuelven 0 filas en silencio**; `limit` y `orden` **se ignoran**. Pero **sí se puede volcar la tabla completa**: `lectura`+`fecha` de 5,566 filas en **186 ms**, y las 21 columnas de las 5,566 filas en **989 ms / 3.4 MB**. El comentario histórico de "~28 s" en `medicionesReales` ya no corresponde. Por eso el archivado **no** se limita a "pedir la última lectura cada tanto" (que es lo que perdía mediciones intermedias): pide el índice completo y recupera **todo** lo que falte.
    - **`archivarTelemetria` (función nueva)**: recorre todos los equipos con `tabla_bd_externa`, **aísla el error de cada uno** (un medidor caído no detiene a los demás), calcula los faltantes **por diferencia contra lo que el origen sí tiene** — un salto en la numeración no se cuenta como pérdida —, trae el detalle, escribe con `bulkCreate` en bloques de 100 con un tope de 1,200 por corrida (un rezago grande se drena en varias corridas en vez de fallar entera), y **mueve el punto de avance sólo después de escribir**. El punto de avance es el mayor consecutivo tal que **todo** lo anterior está guardado, no "la última que escribí" — eso mentiría en cuanto exista un hueco por debajo.
    - **Entidades nuevas**: `ArchivadoEstado` (punto de avance, total archivado, último error por equipo) y `ArchivadoCorrida` (bitácora: inicio, fin, duración, origen, equipos ok/con error, guardadas, detalle por equipo). La bitácora **se abre antes de trabajar**, para que una corrida que se cuelgue no se confunda con una que nunca arrancó.
    - **Respaldo de registros antes de migrar**: `respaldos/LecturaHistorica-20260906T091935Z.json` (374 registros). Un checkpoint de código no protege los datos.
    - ⚠️ **Requiere publicar** y **queda pendiente de comprobación**: la automatización no se registra hasta que se publique, así que todavía **no hay evidencia de una corrida programada real**. La aceptación es ver aparecer registros de `ArchivadoCorrida` con `origen: "programado"` sin nadie usando la app.

59. [x] **Cerradas dos entradas anónimas con privilegios**. Comprobado con una llamada real sin sesión a `https://base44.app/api/apps/{appId}/functions/...`:
    - `guardarLecturaHistorica` respondía **HTTP 200 sin autenticación** y por dentro escribe y borra con `asServiceRole`. Además, cada llamada golpea **dos veces** el servidor del medidor, que no es nuestro: servía de amplificador para saturarlo. Ahora exige sesión (cualquier usuario autenticado, para no romper la ingesta del navegador durante la transición).
    - `tgAiQuery` también respondía sin sesión, y **cada llamada consume el presupuesto de modelo de la cuenta** — cualquiera con el id de la app podía gastarlo. Ahora exige sesión.

60. [x] **La corriente en 0.000 A: investigada desde el origen, y el diagnóstico anterior era incorrecto**. Se había reportado "0.000 A desde julio, revisar la pinza (CT)". Al analizar las 5,567 lecturas del origen, eso resultó **falso en la fecha y prematuro en la causa**:
    - **La corriente SÍ se ha medido**: 868 de 5,567 lecturas (15.6%) traen `IFase1` distinto de cero. La **última fue el 4/sep/2026 a las 11:59:32**, no en julio.
    - **Las magnitudes están cerca del piso de resolución**: mediana 0.021 A, máximo 1.489 A. A ~120 V, 0.021 A son unos 2.5 W — carga fantasma. Un cero puede ser ausencia real de carga tanto como un límite del instrumento, así que **atribuirlo a la pinza era saltarse un paso**. El texto del reporte ahora dice eso y ordena las comprobaciones: primero descartar que el circuito esté sin carga.
    - `kWh` sí avanzó en el periodo (6 → 10), y `THD_IST1` siguió reportando distinto de cero hasta el 4/sep 17:04 — **después** de la última corriente distinta de cero, lo cual es una inconsistencia interna del propio equipo que conviene preguntarle a Boris.
    - 🔴 **Hallazgo nuevo y más grave: las fases 2 y 3 dejaron de reportar tensión.** Reportaron durante meses (mediana **121.0 V** y **122.9 V** sobre 5,130 lecturas) y **cesaron el 2/sep/2026 a las 13:52:25** (lectura 12056). Sus últimos valores **no fueron normales sino 53.4 V y 54.4 V** — ya venían caídas, con V1 también deprimida (~106–108 V). Desde entonces, 419 lecturas en cero. La plataforma **no mostraba nada de esto**: la lógica llamaba a esos canales "sin conectar" y los borraba del análisis.
    - **Corregido en `analisisElectrico.js`**: `canalConectado()` (que afirmaba una causa) se sustituyó por `estadoCanal()`, que distingue `reporta` / `intermitente` / `ceso` / `sin_tension_en_el_periodo` / `sin_lecturas`. Una fase que cesó ahora genera un hallazgo **con fecha y último valor**, y la acción reconoce que desde la app **no se puede distinguir** entre fase fuera de servicio, canal desconectado y falla del medidor. Fijado con 7 comprobaciones nuevas en `npm run test:reporte`.
    - **Banda de 114–127 V: procedencia identificada, con evidencia de que el límite superior está mal.** Sobre las 5,548 lecturas con tensión: mediana **127.2 V**, p95 128.1 V, máximo 137.5 V, y **el 51.0% cae entre 127 y 132 V**. Con la banda actual, **el 55.4% del histórico queda marcado fuera de rango**. El límite inferior cuadra con 127 × 0.90 = 114.3 (−10% de un nominal de 127 V); el superior es **el nominal mismo**, no nominal +10% (139.7). Todo apunta a que se tomó la banda de ±10% y se dejó el nominal arriba por error. **No se cambió el valor**: mover un límite de operación eléctrica corresponde a Boris, no a la interfaz. Lo que sí se corrigió es la etiqueta, que ya no la presenta como certificación de cumplimiento ni de seguridad.

Nota de mirroreo: `archivarTelemetria/entry.ts` vive en la app (Base44) y no se mirroa aquí por tamaño; su contrato y sus decisiones quedan descritos arriba y su `function.jsonc` sí se mirroa, porque es el que documenta el mecanismo de programación de la plataforma.

61. [x] **Revisión independiente del archivado: cinco defectos reales, corregidos**. Una revisión externa encontró que la v1 del archivado no estaba lista. Se verificó cada hallazgo contra la plataforma antes de tocar nada, y todos resultaron ciertos.
    - ✅ **Verificado en producción**: la función `archivarTelemetria` **sí está desplegada** (responde `401`, no `404`, a una llamada sin sesión) y las **tres entradas anónimas con privilegios están cerradas** — `guardarLecturaHistorica`, `tgAiQuery` y `archivarTelemetria` devuelven `401` sin sesión. Eso es evidencia de producción, no de compilación.
    - ❌ **Pero la automatización no ha corrido nunca**: `ArchivadoCorrida` está vacía. Escribir `function.jsonc` y publicar **no bastó** para registrar la tarea programada. No hay herramienta de automatizaciones por MCP ni CLI autenticado en este entorno, y la documentación oficial está bloqueada por el proxy de red, así que **el registro tiene que comprobarse y, si falta, crearse desde el panel de Base44**. Queda identificado como acción manual concreta del dueño.
    - 🔴 **Evidencia de origen falsificable — el defecto más grave, y era mío.** La v1 etiquetaba como `programado` cualquier llamada cuyo cuerpo no dijera otra cosa: invocarla a mano con `{}` **fabricaba una prueba falsa de ejecución automática**. Ahora lo que manda el cliente se guarda como `origen_declarado` y está marcado explícitamente como *sin valor probatorio*; el origen se juzga con `origen_evidencia` (cabeceras no secretas, filtrando `authorization`/`cookie`/`token`/`key`/`secret`) más la identidad autenticada real. La interfaz muestra las dos cosas por separado.
    - 🔴 **Los rechazos ahora dejan rastro.** La v1 devolvía `401`/`403` **antes** de escribir nada, así que una corrida programada que llegara sin la identidad esperada fallaba en silencio y era indiagnosticable. Ahora todo intento queda en la bitácora con `resultado_auth`.
    - 🔴 **Concurrencia**: consultar-y-luego-escribir no protege entre servidores. Se añadió un **arrendamiento por tabla** (`lease_token` + `lease_hasta`) con desempate determinista: ambas ejecuciones escriben su token, releen, y solo continúa aquella cuyo token sobrevivió. **Se documenta su límite**: es optimista sobre un almacén de última-escritura-gana, no una transacción; un arrendamiento caducado se puede tomar para que una corrida muerta no bloquee la tabla para siempre.
    - 🔴 **Fallos silenciosos**: se añadió `timeout` de 20 s por petición, 2 reintentos acotados (sin reintentar lo que va a fallar igual), presupuesto global de 90 s por corrida, y validación de estado HTTP y **forma** de la respuesta. Ahora se distingue **tabla vacía** (respuesta válida) de **respuesta inválida**, de **error del origen** y de **timeout**, cada una con su clase de error.
    - 🔴 **Números como texto**: el origen puede mandar `"1175"`. Antes se descartaban en silencio. Ahora se convierten explícitamente, y un valor no finito (`NaN`, `Infinity`) **nunca se guarda como 0** — se guarda como no disponible, porque un 0 inventado es una medición falsa.
    - 🔴 **Credenciales**: se leen de Secrets (`TG_FUENTE_URL`, `TG_FUENTE_SERVIDOR`, `TG_FUENTE_BASE`, `TG_FUENTE_USUARIO`, `TG_FUENTE_PASSWORD`, `TG_FUENTE_TOKEN`). Mientras no existan se usa el valor del código y **la bitácora lo marca** con `credenciales_del_codigo: true`, para no cortar la ingesta antes de tener el reemplazo. Se anota lo que no se arregla borrándolas: **siguen en el historial de git, así que Boris tiene que rotarlas igualmente.**
    - 🔴 **`guardarLecturaHistorica`**: exigir sesión no bastaba. Se **retiró por completo** la opción `barrerDuplicados` — era una operación destructiva con privilegios de servicio disparada por una bandera del cliente — y la `tabla` **ya no se acepta por confianza**: tiene que corresponder a un `Dispositivo` dado de alta, o responde `404`.
    - ✅ **Exposición de las entidades, verificada**: `read: {}` no significa acceso público. Una consulta sin sesión a `/entities/LecturaHistorica`, `ArchivadoCorrida` y `Dispositivo` devuelve **`403 "This app is private"`**. La exposición real es "cualquier usuario de TG One", que es una decisión del dueño, no un agujero.
    - **`logica.ts` (nuevo)**: la lógica pura (conversión de números, punto de avance, pendientes, decisiones del arrendamiento) vive en un módulo que **importan tanto la función como las pruebas**, para que no puedan desincronizarse. **`npm run test:archivado`**: 33 comprobaciones sobre ingesta repetida, interrupción tras un lote parcial, escritura salteada y dos ejecuciones simultáneas. **`npm run check:funciones`**: compila las 13 funciones de backend — porque compilar el frontend no dice nada del backend.
    - **`EstadoArchivado.jsx` (nuevo, en Inicio)**: muestra la última corrida leída **del servidor**, con qué identidad se ejecutó, cuántas lecturas guardó, cuántas descartó, y por equipo el avance, lo pendiente y el último error. Si no hay ninguna corrida **lo dice**, en vez de dejar un hueco mudo que se lea como "todo bien".

62. [x] **Los tres fallos de móvil: causas encontradas y corregidas**. Alejandro reportó que en el celular *"muchas cosas se pegan, le picas a la equis y no se cierra bien, las cosas se hacen zoom por sí solas y no las puedes regresar"*. No eran síntomas difusos: cada uno tenía una causa localizable.
    - 🔴 **"La equis no cierra" y "se pega" tenían la MISMA causa: `DialogContent` no tenía altura máxima ni scroll propio.** Al estar centrado con `translate-y-[-50%]`, un formulario más alto que el teléfono se cortaba **por arriba y por abajo**: el contenido quedaba inalcanzable y la equis, anclada al borde superior, **se salía físicamente de la pantalla**. Ahora la tarjeta nunca pasa de `calc(100dvh-2rem)`, el contenido rueda dentro de un envoltorio con `min-h-0 flex-1 overflow-y-auto overscroll-contain`, y la equis queda fija fuera del área que se desplaza. `min-h-0` es lo que hace que un hijo flex pueda encogerse: sin él el scroll no aparece nunca.
    - 🔴 **La equis medía 16 px.** Un icono de `h-4 w-4` sin relleno es un blanco casi imposible de acertar con el dedo — parte de "le picas y no se cierra" era literalmente no atinarle. Ahora es un blanco de 40 px con `aria-label="Cerrar"` (además estaba en inglés dentro de una app en español).
    - 🔴 **El zoom automático era el `Select`.** Safari en iOS amplía la página al enfocar cualquier control de menos de 16 px **y no lo deshace al salir**. `Input` y `Textarea` ya usaban `text-base` con `md:text-sm`; el `SelectTrigger` se había quedado en `text-sm` fijo. Corregido, más una red de seguridad en CSS para todos los controles de formulario en móvil. **No se bloqueó el zoom manual**: `user-scalable=no` arreglaría el síntoma rompiendo la accesibilidad de quien necesita acercar.
    - 🔴 **El mapa atrapaba el dedo.** Leaflet se queda con el gesto de un dedo por omisión y, como el mapa ocupa todo el ancho, no se podía seguir bajando la página — la misma trampa que ya se había corregido en el visor 3D. Ahora en pantalla táctil arranca quieto, se toca para activarlo y hay un botón **"Listo"** para devolver el gesto a la página. **Detalle que casi se escapa**: react-leaflet solo lee props como `dragging` **al crear** el mapa, así que cambiarlas después no hace nada; hubo que actuar sobre la instancia con `map.dragging.enable()/disable()`.
    - **`SheetContent`** tenía los mismos defectos: sin scroll interno y sin tope de alto en los paneles superior/inferior. Corregido igual.
    - **Otros arreglos de tacto**: `overscroll-behavior: contain` para que al llegar al final de un panel el gesto no arrastre la página de atrás; `-webkit-overflow-scrolling: touch`; mínimo de 40 px en botones táctiles; y `text-size-adjust: 100%` para que iOS no reescale el texto al girar el teléfono.
    - ⚠️ **Sin verificar en un dispositivo real.** No hay navegador en el entorno de trabajo ni en el sandbox de Base44, y `base44.app` no es alcanzable desde este contenedor. Las causas están identificadas en el código y corregidas, pero **quien lo reportó es quien puede confirmarlo**: los mismos tres gestos en el celular.

63. [x] **Que la app deje de sentirse muerta al tocarla**. *"Se ve muy aburrido"* en un teléfono suele ser falta de acuse de recibo: tocas algo y no pasa nada hasta que carga la pantalla siguiente, y medio segundo sin respuesta se lee como que la app se colgó.
    - **Barra de navegación inferior (`BarraInferior.jsx`)**, solo en teléfono: Inicio, Medición, Equipos, TG AI y Menú. Antes, para ir a cualquier sitio había que abrir el menú lateral, buscar dentro y cerrarlo — cuatro toques para algo que se hace veinte veces al día. Respeta `env(safe-area-inset-bottom)`, porque en iPhone la franja del gesto de inicio se come la última fila.
    - **Respuesta al tacto**: se quita el destello azul del sistema y se sustituye por un hundido breve (`scale(.97)`, 90 ms) consistente en botones, enlaces y pestañas.
    - **Valores en vivo (`useValorNuevo.js`)**: la cifra protagonista se realza un instante cuando llega una medición **nueva de verdad**. Se engancha al consecutivo de la lectura, **no al render**: la pantalla se repinta por muchas razones que no son el medidor, y realzar en todas ellas sería ruido que enseña a la gente a ignorar el realce. No se dispara con el primer valor, porque al abrir la pantalla todo es "nuevo" y encenderla entera no informa de nada.
    - Todo lo anterior se desactiva bajo `prefers-reduced-motion`.
    - **Bug propio atrapado por `no-undef`**: una llamada a `edit_file` que devolvió *timeout* **sí había completado del lado del servidor** y pisó una escritura posterior mía, dejando `OpsMap.jsx` con JSX que referenciaba un estado inexistente. El lint lo detuvo antes de publicar — es la segunda vez que esa regla, activada tras el incidente de `useMemo`, atrapa un fallo que el build sí dejaba pasar.

Nota: `AiCopilot.jsx`, `Soporte.jsx`, `DispositivoDetalle.jsx`, `DashboardEjecutivo.jsx`, `OpsMap.jsx`/`Home.jsx`, `TotalView.jsx`/`MonitorShell.jsx`/`monitorReport.js`, `Locaciones.jsx`/`ActivoDetalle.jsx`/`App.jsx`, `guestSupportRequest`, `guestRead`/`guest.js`/`Invitado.jsx`/`Login.jsx`/`Presentacion.jsx`, `tgAiQuery`, `Sidebar.jsx`/`Layout.jsx`, `EstadoSalud.jsx`/`InfoTecnico.jsx`/`AyudaTgOne.jsx`, `Catalogo.jsx`/`ProductoCard.jsx`/`ProductoDialog.jsx`/`PortafolioWidget.jsx`/`FamiliaConfig.jsx` (eliminados), `AuthContext.jsx`/`authReturnTo.js`, `Logo.jsx`/`logoDatos.js`/`generar-logo.py`, `ReportGenerator.jsx`/`reportePdf.js`, `emitirInvitacion.entry.ts`/`AltaUsuarios.jsx`, `Instrumento.jsx`/`BarraEstado.jsx`/`PanelEquipo.jsx`/`ColumnaCopiloto.jsx`/`use-toast.jsx`, `Escena3D.jsx`/`modeloTOV452.js` y `SelectorEquipo.jsx`/`Layout.jsx` son componentes/funciones de TG One en general, no exclusivos de la integración TOV452 — por eso no se mirroan como archivos en `backend/`/`frontend/` de este repo (que documenta específicamente el contrato de telemetría), pero se registran aquí porque todos los cambios de esta sesión de trabajo tocaron la misma app en vivo. `pruebaCrud.entry.ts`, `PruebaRegistros.jsx`, `guardarLecturaHistorica.entry.ts`, `LecturaHistorica.entity.jsonc`, `TendenciasHistoricas.jsx`, `LocacionDetalle.jsx`, `estadoSalud.js`, `glosario.js`, `analisisElectrico.js`, `frescura.js`, `prueba-reporte.mjs` y `prueba-frescura.mjs` sí se mirroan porque documentan o ejercitan directamente el contrato/los valores reales de telemetría — `frescura.js` en particular fija cómo se interpreta el campo `fecha` que manda el medidor (`"2026-09-02 18:23:47"`, sin zona) y por qué la hora de la medición nunca debe presentarse como la hora de la consulta.

### Verificación previa a revisión (22/jul/2026)

Antes de entregar se corrió, sobre el código ya desplegado:
- `npm run lint` en todo el proyecto — 0 errores (se corrigieron también ~12 imports sin usar preexistentes en archivos no relacionados, para dejar el proyecto completo en cero).
- `npm run build` — compila sin errores.
- `npm run typecheck` tiene ~543 errores preexistentes en todo el proyecto (tipado laxo de componentes UI compartidos, de antes de esta integración) — no son nuevos ni bloquean el build; no se tocaron por estar fuera del alcance de esta integración.
- Se confirmó en el código fuente de `react-router-dom` que `NavLink` soporta `children` como función (usado en el rediseño del sidebar para pintar el ícono según el estado activo) — no es una suposición.

`useTovLive.js` queda sin ninguna página que lo importe — es código muerto, no rompe nada, se deja documentado como historial.

## Nota de seguridad

El endpoint real sigue sin distinguir lectura de escritura por intención, solo por verbo HTTP, y ya hubo un incidente real de borrado accidental. Por eso:
- `medicionesReales` (usada por TG One) nunca manda nada distinto de `action: 'get'` — no hay forma de escribir desde ahí.
- La consola de pruebas (`dbcommQuery`, en la app "DB Data Connector") bloquea cualquier payload con la clave `campos` (la que dispara un INSERT/UPDATE reales) salvo que quien la ejecute tenga rol `admin` **y** mande `confirmarEscritura: true` explícitamente.
- `PUT`/`DELETE` contra el servidor real siguen sin exponerse desde ninguna de las dos apps.
